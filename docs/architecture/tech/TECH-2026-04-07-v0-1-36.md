> Migrated from `docs/release/2026-04-07-v0.1.36-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.36 迭代记录

## 迭代摘要

本轮迭代聚焦 Step 03 的工具链治理收口，不再停留在临时命令层，而是把已经验证可用的能力沉淀为正式工程入口。目标是让后续重复执�?`/docs/prompts/反复执行Step指令.md` 时，依赖修复与安全测试都具备稳定、可复用、可审计的落地方式�?
## 本轮交付

### 1. 正式固化 workspace 依赖修复入口

- 在根脚本新增 `repair:deps`
- �?`scripts/repair-workspace-node-modules.mjs` 作为正式工作流入口使�?- 保持 `preflight:deps` 只做检测，不在未知状态下隐式改写 `node_modules`

### 2. 正式固化安全 Vitest 入口

- 新增 `scripts/run-vitest-safe.mjs`
- 根脚�?`test:vitest:safe` 改为调用单独�?Node 包装器，而不是把安全参数直接写死�?package script �?- 包装器统一注入以下安全参数�?  - `--configLoader native`
  - `--pool threads`
  - `--maxWorkers 1`
  - `--exclude ".worktrees/**"`
- 包装器会主动清洗 `pnpm run ... -- <files>` 传入的前�?`--`，避�?Windows 下参数转发异常导致误跑整�?
### 3. 升级依赖预检提示

- `scripts/ensure-workspace-node-modules.mjs` 的异常提示改为双阶段处置�?  - 先执�?`pnpm run repair:deps`
  - 若仍存在缺失，再在当�?workspace 内执�?`pnpm install --frozen-lockfile`
- 明确禁止把根因模糊化为“直�?reinstall 即可�?
### 4. 新增回归测试约束

- 新增 `scripts/__tests__/run-vitest-safe.test.ts`
- 扩展 `scripts/__tests__/ensure-workspace-node-modules.test.ts`
- 扩展 `tests/testRunnerBoundary.test.ts`
- 保留并复�?`scripts/__tests__/repair-workspace-node-modules.test.ts`

### 5. 修正交付可追踪�?
- 更新 `.gitignore`
- 明确放行 `scripts/run-vitest-safe.mjs`
- 明确放行 `docs/release/*.md`
- 避免本轮正式脚本�?release 文档被忽略，导致交付物只存在于本地工作区

## 本轮验证

### 已通过的验�?
- `node node_modules/vitest/vitest.mjs run --config tests/vitest.codex.config.mjs --configLoader native --pool threads --maxWorkers 1 --exclude ".worktrees/**" scripts/__tests__/run-vitest-safe.test.ts scripts/__tests__/ensure-workspace-node-modules.test.ts scripts/__tests__/repair-workspace-node-modules.test.ts tests/testRunnerBoundary.test.ts`
  - 结果：`4 files, 14 tests passed`
- `pnpm.cmd run test:vitest:safe -- scripts/__tests__/run-vitest-safe.test.ts scripts/__tests__/ensure-workspace-node-modules.test.ts scripts/__tests__/repair-workspace-node-modules.test.ts tests/testRunnerBoundary.test.ts packages/sdkwork-magic-studio-voicespeaker/src/components/VoiceLeftGeneratorPanel.boundary.test.ts packages/sdkwork-magic-studio-voicespeaker/src/components/voicespeaker/VoiceLabModal.boundary.test.ts packages/sdkwork-magic-studio-character/src/components/CharacterLeftGeneratorPanel.boundary.test.ts`
  - 结果：`7 files, 17 tests passed`
- `pnpm.cmd run repair:deps`
  - 结果：命令成功执行，workspace 入口已产品化
- `git status --short -- .gitignore package.json scripts/run-vitest-safe.mjs docs/release/CHANGELOG.md docs/release/VERSION.md docs/release/2026-04-07-v0.1.36-迭代记录.md`
  - 结果：本轮交付文件已可见于版本控制候选集，不再被 `.gitignore` 吞掉

### 当前依赖修复状�?
- `workspaceExisting: 39`
- `regularExisting: 101`
- `transitiveCreated: 0`
- `transitiveExisting: 0`
- `missingCount: 75`
- `ambiguousCount: 6`

### 典型未恢复依赖样�?
- `jszip`
- `mediabunny`
- `tippy.js`
- `@eslint/js`
- `archiver`
- `png-to-ico`
- `sharp`
- `typescript-eslint`

## 本轮结论

- 已完成：安全 Vitest 执行链路正式化、参数转发治理、依赖预检提示治理、依�?relink 入口正式�?- 未完成：当前 workspace �?`.pnpm` 存量仍不完整，整�?`test / typecheck / build` 仍未恢复
- 阶段判断：Step 03 被进一步推进，但未完成；当前只能声明“工具链治理闭环部分完成”，不能声明“仓库恢复完成�?
## 风险与边�?
- `repair:deps` 当前只能重建本地虚拟仓中已经存在的包链接，不能凭空补�?`.pnpm` 中缺失的�?- 本轮没有重新宣称整库 `test`、整�?`typecheck`、整�?`build` 通过
- 本轮没有宣称达到 `alpha`
- 本轮没有修改默认 `test:vitest`，避免把性能和环境策略直接强行套用到所有场�?
## 下一步建�?
1. 以当�?`missingCount=75` 为输入，按“可补链�?/ 必须补包 / 平台可选包”三类拆分恢复清单�?2. 继续使用 `test:vitest:safe` 作为 Step 执行过程中的标准回归入口，逐步扩大回归集，而不是直接跑整库�?3. �?`.pnpm` 缺失恢复与“整库失败测试分流”拆成可并行子任务推进，避免互相阻塞�?
