> Migrated from `docs/release/2026-04-07-v0.1.21-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.21 迭代记录

## 迭代主题

Step 03 第十七轮局部闭环：

- �?`ChooseAsset` 建立最�?`projectReference` 能力
- 打�?`video-studio / SubjectReferenceSection` �?project-level persisted reference 回写
- 让真实视频主体参考图上传首次进入 asset-center persisted reference 主干

## 当前 Step / Wave

- 当前 Step: `Step 03`
- 当前 Wave: `Wave A / 共享主干收敛`
- 当前版本: `v0.1.21`
- Release Stage: `internal`

## 本轮交付

### 代码变更

- 新增 `packages/sdkwork-magic-studio-assets/src/components/chooseAssetProjectReference.ts`
  - 收敛 `ChooseAsset` 上传�?project-level persisted reference �?scope / reference / locator / metadata / status 解析
- 更新 `packages/sdkwork-magic-studio-assets/src/components/ChooseAsset.tsx`
  - 增加可�?`projectReference`
  - 本地上传成功后补 `registerExistingAsset(...) / bindReference(...)`
- 更新 `packages/sdkwork-magic-studio-assets/src/choose-asset/index.ts`
  - 导出 `ChooseAssetProjectReference` 类型
- 更新 `packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.tsx`
  - 为视频主体参考图显式传入 `slot: subject-reference`
  - 追加 `source: subject-reference-section` 元数�?
### 测试变更

- 新增 `packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx`
  - 覆盖 `registerExistingAsset(...)`
  - 覆盖 `bindReference(...)`
- 更新 `packages/sdkwork-magic-studio-assets/tests/chooseAssetIdentity.test.tsx`
  - 改为 mock 真实导入路径，消�?`indexedDB is not defined` 假阳�?- 新增 `packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.boundary.test.ts`
  - 固化真实消费方已经声�?`projectReference` 的边界证�?
### 文档变更

- 新增 `docs/review/2026-04-07-step-03-choose-asset-subject-reference-binding.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 为什么做这一�?
上一轮已经把 `assetCenterClient.ts` �?shared SDK 主干 blocker 解除，但 `Step 03` 仍然保留一个明确未闭环点：

- `ChooseAsset` 在多个真实业务入口里只做�?SDK 导入
- 没有把资产绑定为当前项目�?persisted reference

这意味着�?
- 资产虽然能上传成�?- 但删除前引用阻断与项目级资产审计仍然看不到这条真实业务使用关�?
本轮不泛化整�?`ChooseAsset` 消费面，只先�?`SubjectReferenceSection` 做最小闭环，确保有一条真实视频业务入口先进入统一资产协议�?
## 真实验证

### RED

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.test.tsx --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `ChooseAsset` persisted reference 用例失败，证明当前上传链路没有进�?asset-center persisted reference 主干
- 同轮暴露 `SubjectReferenceSection` 运行�?TSX import-analysis blocker，因此后续改�?source boundary 固化消费方证�?
### GREEN

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `2 files, 3 tests passed`

### 回归

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/chooseAssetIdentity.test.tsx packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx packages/sdkwork-magic-studio-assets/tests/assetCenterProjectManagedImport.test.ts packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `5 files, 14 tests passed`

### Typecheck

命令�?
```bash
pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-assets/src/components/ChooseAsset.tsx packages/sdkwork-magic-studio-assets/src/components/chooseAssetProjectReference.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.tsx
```

结果�?
- `PASS`

## 版本结论

### 本轮结论

- `v0.1.21` 有效
- `internal` 持续通过
- `ChooseAsset` 首次具备可�?project-level persisted reference 写回能力
- `video-studio / SubjectReferenceSection` 首次真实进入 asset-center persisted reference 主干

### 达成的能�?
- `ChooseAsset` 不再只能“上传成功然后把 asset 回传�?UI�?- 当消费方显式声明 `projectReference` 时，本地上传会自动补�?  - `registerExistingAsset(...)`
  - �?`bindReference(...)`
- 视频主体参考图现在具备明确的项目级引用关系，可被删除保护和后续审计识别

### 尚未达成

- `ChooseAsset` 全量消费面还没有接入
- `StartEndFramesSection / LipSyncSection / 其他 ChooseAsset` 入口仍待补齐
- `Step 03` 不能宣告完成
- `alpha` 不能开�?
## 后续建议

1. 下一轮继续锁�?`Step 03`，优先处�?`StartEndFramesSection` �?`start-frame / end-frame` 两个槽位�?2. 再处�?`LipSyncSection` �?`source-video / source-image / driver-audio` 三个槽位，避免视频模式内�?persisted reference 语义继续碎片化�?3. �?video 包当�?TSX import-analysis blocker 记录为测试基础设施并行 lane，但不要拿它掩盖 persisted reference 业务闭环是否完成的事实�?
## 商业化影响评�?
这一轮虽然只覆盖了一个真实视频入口，但它的价值是明确的：

- `ChooseAsset` 从纯 UI 选择器向“可选纳入统一资产治理协议”的组件边界迈进了一�?- 视频主体参考图从“可上传”提升到“可治理、可审计、可阻断误删�?- 后续再补 `StartEndFrames / LipSync / VoicePersona` 时，可以沿同一 helper 继续收敛，而不是在各业务面散落实现

