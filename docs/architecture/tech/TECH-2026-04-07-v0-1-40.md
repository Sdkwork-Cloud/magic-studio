> Migrated from `docs/release/2026-04-07-v0.1.40-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.40 迭代记录

## 迭代概述

本轮聚焦 `Wave A / Step 03` 的依赖恢复治理，目标是继续压�?`repair:deps` 的误报范围，并确�?`safe Vitest` 边界不回退�?
## 对应 Step / Wave

- Current Wave: `Wave A`
- Current Step: `Step 03`
- 本轮主题：`虚拟仓权限回退�?OR semver 修复`

## 本轮完成内容

### 1. 修复虚拟仓权限回退

- �?`scripts/repair-workspace-node-modules.mjs` 中为 `.pnpm` 候选包增加 `EPERM / EACCES` 读取失败回退
- 当候选包 `package.json` 无法直接读取时，改为使用虚拟仓目录名中的版本信息继续匹配与排�?
### 2. 修复 OR semver 范围解析

- �?`satisfiesDependencyRange(...)` 增加 `||` 范围支持
- 修复 `zod`、`zod-validation-error` 等依赖在 OR 范围下被错误判缺的问�?
### 3. 补齐回归测试

- 新增 `EPERM` 候选回退用例
- 新增 `||` semver 范围用例
- 保持已有 `prerelease` �?`safe runner` 相关测试继续通过

## 核心结果

执行 `repair:deps` 后，frontier �?`21` 收敛�?`11`�?
- `actionableUniqueCount: 11`
- `frontierUniqueCount: 11`
- `deferredUniqueCount: 0`

剩余 11 项如下：

- `@eslint/js`
- `archiver`
- `fast-xml-parser`
- `mediabunny`
- `png-to-ico`
- `protobufjs`
- `rxjs`
- `sharp`
- `shell-quote`
- `tippy.js`
- `typescript-eslint`

## 测试与验�?
### 定向测试

- `pnpm.cmd run test:vitest:safe -- scripts/__tests__/repair-workspace-node-modules.test.ts`
  - 结果：`8 tests passed`

### Step 03 回归测试

- `pnpm.cmd run test:vitest:safe -- scripts/__tests__/run-vitest-safe.test.ts scripts/__tests__/ensure-workspace-node-modules.test.ts scripts/__tests__/repair-workspace-node-modules.test.ts tests/testRunnerBoundary.test.ts packages/sdkwork-magic-studio-voicespeaker/src/components/VoiceLeftGeneratorPanel.boundary.test.ts packages/sdkwork-magic-studio-voicespeaker/src/components/voicespeaker/VoiceLabModal.boundary.test.ts packages/sdkwork-magic-studio-character/src/components/CharacterLeftGeneratorPanel.boundary.test.ts`
  - 结果：`7 files, 23 tests passed`

### 诊断结论

- `fast-xml-parser`
- `rxjs`
- `shell-quote`

�?`.pnpm` 根目录下可见目录名，但期望路径下不存在可读取�?`package.json`，本轮判定为虚拟仓内容缺失，而不是脚本误报�?
## 风险与限�?
1. `CI=true + pnpm install --offline --ignore-scripts --frozen-lockfile --filter .` 仍然�?`No-Go` 路径�?2. Step 03 仍未完成，不能提升到 `alpha`�?3. 尚未完成 full build / full typecheck / full release 级验证�?
## 下一步计�?
1. 继续治理剩余 11 �?frontier�?2. 区分“真实安装缺口”和“虚拟仓内容破损”两条处理路径�?3. 保持 `repair:deps` �?`test:vitest:safe` 作为每轮 Step 03 的必跑校验�?
