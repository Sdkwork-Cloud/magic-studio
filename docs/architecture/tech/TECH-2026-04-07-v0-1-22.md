> Migrated from `docs/release/2026-04-07-v0.1.22-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.22 迭代记录

## 迭代主题

Step 03 第十八轮局部闭环：

- 继续复用 `ChooseAsset.projectReference` 主干能力
- 打�?`video-studio / StartEndFramesSection`
- �?`Start Image / End Image` 两个真实入口补齐项目�?persisted reference 绑定

## 当前 Step / Wave

- 当前 Step: `Step 03`
- 当前 Wave: `Wave A / 共享主干收敛`
- 当前版本: `v0.1.22`
- Release Stage: `internal`

## 本轮交付

### 代码变更

- 更新 `packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.tsx`
  - 为起始帧 `ChooseAsset` 增加 `slot: 'start-frame'`
  - 为结束帧 `ChooseAsset` 增加 `slot: 'end-frame'`
  - 两个槽位统一携带 `source: 'start-end-frames-section'`

### 测试变更

- 新增 `packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.boundary.test.ts`
  - 固化两个真实帧槽位已经接�?`projectReference` 的边界证�?
### 文档变更

- 新增 `docs/review/2026-04-07-step-03-start-end-frames-reference-binding.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 为什么做这一�?
上一轮已经把 `ChooseAsset` �?project-level persisted reference 能力�?`SubjectReferenceSection` 闭环打通，�?`StartEndFramesSection` 依然保留两个真实业务盲区�?
- 起始帧可以上传，但不会被登记为项目级引用
- 结束帧可以上传，但不会被登记为项目级引用

这会直接影响�?
- 项目级资产引用审�?- 删除保护
- 多帧视频生成输入的治理一致�?
本轮的目标不是扩大范围，而是继续按真实入口一个个收口，保持证据可验证、实现可复用�?
## 真实验证

### RED

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 test | 1 failed`
- 失败原因：当前源码没有声�?`projectReference`、`start-frame`、`end-frame`

### GREEN

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 file, 1 test passed`

### 回归

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/chooseAssetIdentity.test.tsx packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx packages/sdkwork-magic-studio-assets/tests/assetCenterProjectManagedImport.test.ts packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `6 files, 15 tests passed`

### Typecheck

命令�?
```bash
pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-assets/src/components/ChooseAsset.tsx packages/sdkwork-magic-studio-assets/src/components/chooseAssetProjectReference.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.tsx packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.tsx
```

结果�?
- `PASS`

## 版本结论

### 本轮结论

- `v0.1.22` 有效
- `internal` 持续通过
- `video-studio / StartEndFramesSection` 首次进入项目�?persisted reference 主干
- `Step 03` 继续进行中，未结�?
### 已达成能�?
- `Start Image` 上传后具备明确的项目级引用语�?- `End Image` 上传后具备明确的项目级引用语�?- 两个槽位与视频请求构建主干使用统一术语，不再是 UI 自说自话的孤立上传入�?
### 尚未达成

- `LipSyncSection` 仍未接入
- 其他 `ChooseAsset` 消费面仍未全量审�?- 全仓 `test / typecheck / build` 尚未验证
- `alpha` 仍不能开�?
## 后续建议

1. 下一轮继续锁�?`Step 03`，优先处�?`LipSyncSection`�?2. �?`source-video / source-image / driver-audio` 三个真实槽位补齐项目�?persisted reference�?3. 完成 `LipSyncSection` 后，再重新评�?`ChooseAsset` 消费面是否具备进入下一阶段的条件�?
## 商业化影响评�?
这一轮虽然仍然是局部收敛，但价值明确：

- 多帧视频输入首次具备项目级资产治理语�?- 视频创作模式中“开始帧 / 结束帧”从可上传提升为可治理、可审计
- 后续扩展更多视频模式时，可以继续复用同一 `projectReference` 协议，而不是回退到分散实�?
