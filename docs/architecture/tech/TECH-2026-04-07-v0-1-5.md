> Migrated from `docs/release/2026-04-07-v0.1.5-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.5 迭代记录

## 本轮目标

继续�?`docs/prompts/反复执行Step指令.md` 执行当前�?step，不停留在文档层，选择 Step 03 中最危险、最容易影响商业化交付可信度的一条真实缺口先闭环�?
- 资产中心删除回收必须限制�?Magic Studio 受管目录�?- 外部本地文件不能因为被索引引用就被直接物理删�?
## 当前 Step / Wave

- 当前 Step: `03-资产中心与本地存储闭环`
- 当前 Wave: `Wave A / 共享主干收敛`

## 本轮完成�?
- �?`AssetCenterService.deleteById(...)` 增加受管目录删除门禁
- �?`magicStudioAssetLayout.ts` 增加共享函数 `isManagedAssetAbsolutePath(...)`
- 将系统素材库、项�?`media/cache/exports` 以及独立覆写 `cacheRootDir/exportsRootDir` 统一纳入受管目录判定
- 新增删除安全测试，验证外部绝对路径不会被物理删除
- 回归已有受管导入布局测试，确认目录规划未被本轮安全收敛破�?
## 代码变更范围

- `packages/sdkwork-magic-studio-assets/src/asset-center/application/AssetCenterService.ts`
- `packages/sdkwork-magic-studio-assets/src/asset-center/application/magicStudioAssetLayout.ts`
- `packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts`
- `packages/sdkwork-magic-studio-assets/tests/magicStudioAssetLayout.test.ts`

## 测试与验证结�?
### Red

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads`
  - 结果: `FAIL`
  - 证明�? 外部绝对路径 `/Users/demo/Desktop/imports/raw-resource-1.mp4` 曾被错误删除

### Green / Regression

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads`
  - 结果: `1 file, 2 tests passed`

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/magicStudioAssetLayout.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`
  - 结果: `1 file, 5 tests passed`

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/assetCenterProjectManagedImport.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`
  - 结果: `1 file, 6 tests passed`

- `pnpm.cmd exec tsc --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-assets/src/asset-center/application/AssetCenterService.ts packages/sdkwork-magic-studio-assets/src/asset-center/application/magicStudioAssetLayout.ts`
  - 结果: `PASS`

## review 与架构回�?
- 新增 `docs/review/2026-04-07-step-03-asset-delete-safety.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`
- 本轮未更�?`docs/架构/`
  - 原因: 本轮实现的是既有 Step 03 风险条目的真实落地，没有改动架构标准本身

## 风险与回�?
### 风险

- Step 03 仍未完成统一 `assetId` 主引用收�?- 三大引擎仍未全部切换到资产中心主引用协议
- 外部绝对路径目前只做“索引删除”，后续还需要补齐用户态回收和重定位机�?- 仓库存在 `.worktrees/` 历史副本，Vitest 需要显�?`--exclude ".worktrees/**"` 才能避免同名测试误扫

### 回滚

- 本轮改动仅集中在资产中心删除与布局判定�?- 若需回退，可按文件级回退�?  - `AssetCenterService.ts`
  - `magicStudioAssetLayout.ts`
  - 新增测试文件
- 不涉及数据库迁移、远程接口契约或桌面权限配置回滚

## 对商业化交付的提�?
- 将资产中心从“可导入”推进到“可受控删除�?- 降低了误删用户桌面本地文件的高风险隐�?- 为后续引擎资产接线、回收站策略、引用追踪和本地存储治理提供了可信基础

## 自我反证结论

- 本轮不是“只写一层保�?if”，而是把删除安全边界收敛成共享规则并用测试固化
- 本轮也没有夸大为 Step 03 全量完成，统一 `assetId` 主引用、跨引擎接线和引用追踪仍需后续继续推进
- 相比上轮，当前版本更接近商业化交付，因为本地资产治理开始具备安全边界，而不是默认信任任意索引路�?
## 下一轮计�?
1. 继续执行 `Step 03`，优先统一 `assetId / primaryResourceId / resourceViewId` 的主引用协议
2. �?Film / MagicCut / Canvas 对资产中心解析链进一步收敛，减少裸路径和临时 URL 主引�?3. 补齐资产反向引用追踪、删除前引用检查与用户态回收策�?
