> Migrated from `docs/release/2026-04-07-v0.1.11-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.11 迭代记录

## 本轮目标

继续执行 `docs/prompts/反复执行Step指令.md`，在 `Step 03` 内推进资�?identity 收敛的下一轮真实闭环：

- 修复 Canvas 生成引用链路忽略 `metadata` canonical identity 的问�?- �?Canvas �?image/video 引用构造与统一视频请求中复�?canonical 资产身份，而不是退回本地临�?`uuid`

## 当前 Step / Wave

- 当前 Step: `03-资产中心与本地存储闭环`
- 当前 Wave: `Wave A / 共享主干收敛`

## 本轮完成�?
- 更新 `packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts`
  - 新增 Canvas image/video metadata canonical identity 回归用例
  - 收敛本测试文件的局�?mock 方式，移除会误触发平台副作用�?`vi.importActual(...)`
- 更新 `packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.ts`
  - 增加 Canvas 资源 canonical identity 统一解析逻辑
  - image/video 引用入口都改为复�?`metadata` 中的 canonical `assetId / assetUuid / primaryResourceId / primaryResourceUuid / resourceViewId / resourceViewUuid`
  - �?`metadata` 同步透传�?input ref 顶层
- 新增 `docs/review/2026-04-07-step-03-canvas-generation-metadata-identity.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 代码变更范围

- `packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.ts`
- `packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts`

## 测试与验证结�?
### Red Blocker

首次新增用例后执行：

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`

结果�?
- `0 test`
- 局�?mock 方式误触发：
  - `indexedDB is not defined`
  - `EPERM: operation not permitted, open ... retry/index.js`

处理结论�?
- 该问题仅作为本测试文件的局部测试基础设施 blocker 记录
- 已通过无副作用 mock 清障
- 未宣称仓库级 node/web 隔离问题已经解决

### Red

清障后再次执行同一命令�?
- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`

结果�?
- `1 file, 5 tests`
- `2 failed`

失败原因�?
- image 引用没有�?`metadata` 中的 canonical identity 回填�?top-level ref
- video 请求 `assets[].value` 仍然退回本�?`uuid`

### Green

修复后执行：

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`

结果�?
- `1 file, 5 tests passed`

### Regression

执行主题回归包：

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts packages/sdkwork-magic-studio-canvas/src/utils/canvasGeneratedOutcomeResource.test.ts packages/sdkwork-magic-studio-canvas/src/utils/canvasReferenceImageImport.test.ts packages/sdkwork-magic-studio-magiccut/tests/generatedSelectionImport.test.ts packages/sdkwork-magic-studio-film/tests/generatedSelectionAssetImport.test.ts packages/sdkwork-magic-studio-assets/tests/generatedSelectionAsset.test.ts packages/sdkwork-magic-studio-assets/tests/generatedSelectionAssetPersistence.test.ts packages/sdkwork-magic-studio-assets/tests/generatedOutcomeAssetPersistence.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`

结果�?
- `8 files, 35 tests passed`

## review 与架构回�?
- 本轮已新增：
  - `docs/review/2026-04-07-step-03-canvas-generation-metadata-identity.md`
- 本轮未改�?`docs/架构/` �?`docs/step/`
  - 原因：本轮是 Step 03 既有能力树下的实现补洞，不改变架构边界与 step 规划，只�?evidence 回写

## 风险与回�?
### 风险

- Canvas 导入链路仍有同类 nested canonical identity 缺口待收�?- `ChooseAsset` 更大范围 node/web 平台隔离问题仍未关闭
- Step 03 仍未完成删除前反向引用治�?
### 回滚

- 本轮可按文件级回滚：
  - `packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.ts`
  - `packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts`
- 不涉及数据库迁移、远程契约调整或桌面权限变更

## 对商业化交付的提�?
- 降低 Canvas 在多轮生成引用中因本地临�?`uuid` 造成的身份漂�?- 提高 Canvas 与共享资产中心、统一视频请求之间�?canonical 主引用一致�?- �?Step 03 �?identity 收敛真正推进到第三条创作引擎主干，而不再停留在共享层、Film、MagicCut

## 自我反证结论

- 本轮不是文档层闭环，已经包含真实代码、真实失败测试、真实修复和真实回归验证
- 本轮不是 Canvas 全量闭环，只关闭了“Canvas 作为生成引用时”的 metadata identity 缺口
- 本轮只能定义�?`Step 03 第七轮局部闭环`
- 当前仍不满足 `alpha` 升级条件

## 下一轮计�?
1. 继续停留�?`Step 03`
2. 优先处理 Canvas 导入链路�?`resource / resource.metadata` canonical identity 的同类收�?3. 视风险和写集边界决定是否�?`ChooseAsset` node/web 隔离问题拉成独立并行 lane

