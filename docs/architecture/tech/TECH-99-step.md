> Migrated from `docs/step/99-Step最快并行落地总控手册.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step最快并行落地总控手册

## 1. 文档定位

本文件回�?program 级问题：

`如果要在不牺牲安全、质量、可回滚性和架构一致性的前提下，用最快速度�?00-13 全部落完，具体该怎么组织？`

它不是单 step 文档，而是总控文档�? 
因此它必须同时约束：

- 波次顺序
- 资源配置
- 多子 agent / 多人分工
- 共享文件�?- 例外审批
- 停止并行的条�?
## 2. 强制联读矩阵

总控视角下，以下文档缺一不可�?
| 时刻 | 必读文档 | 目的 |
| --- | --- | --- |
| 开工前 | `README` `90` `92` | 统一事实基线、映射关系、阻塞语�?|
| 并行编排�?| `87` `94` `96` | 冻结责任、lane 边界、周计划 |
| 合流�?| `88` `98` | 统一证明包与交接包格�?|
| 完工�?| `89` `91` `95` `97` | 完工判定、质量审计、闭环验收、回�?|
| 波次收尾 | `93` | 判断是否允许进入下一波次 |

## 3. 当前最短关键路�?
真正的最短关键路径是�?
`00 -> 01 -> 02/03/04/05 -> 06/07/08/09 -> 10/11/12 -> 13`

其中�?
- `00`、`01`、`13` 必须严格串行
- `02-05` 是共享主干冻结期，不能被业务 lane 绕过
- `06-09` 是核心创作闭环期，可以并行，但必须基于冻结协�?- `10-12` 是生态、交付、治理期，可以并行，但必须建立在 Wave B 已有真实主链路的基础�?
## 4. 推荐资源模型

### 4.1 `4 agent` 最小模�?
- G0：总控组，负责 `00` `01` `13`
- G1：共享主干组，按顺序处理 `02-05`
- G2：业务闭环组，按顺序处理 `06-09`
- G3：交付治理组，按顺序处理 `10-12`

适用：资源少、追求稳态，不追求极限速度�? 
风险：总周期较长，但控制最简单�?
### 4.2 `8 agent` 标准模型

- G0：总控组，负责 `00` `01` `13`
- A：`02`，后续承�?`11` 中的项目/同步主干部分
- B：`03`
- C：`04`，后续承�?`09`
- D：`05`，后续承�?`10`
- E：`06`
- F：`07`
- G：`08`，后续承�?`12`

适用：推荐模型�? 
特点：Wave A 可四路并行，Wave B 可三路并行，Wave C �?A/C/D/G 在冻结主干上继续推进�?
### 4.3 `10+ agent` 激进模�?
- �?`8 agent` 标准模型基础�?- 拆出独立�?`09`、`10`、`11`、`12` 专项 agent
- �?`02/03/04/05`、`06/07/08/11` 各自再拆 2-3 个子任务 agent
- 保留一个独�?QA/集成 agent 只做审计与合�?
适用：追求最短周期，且团队具备严�?owner 纪律�? 
前提：必须严格遵�?`87`、`94`、`98` 的写集与交接规则�?
## 5. 每个 Lane 的统一 Preflight

所�?lane 开工前必须记录并冻结以下信息：

1. `base commit SHA`
2. `MAGIC_STUDIO_VITE_MODE`
3. `MAGIC_STUDIO_SDK_MODE`
4. 当前样例工程或测试夹�?5. 当前平台约束
6. 当前 must-run 命令

推荐统一 preflight�?
- `pnpm install`
- `pnpm run preflight:deps`
- `pnpm run typecheck`

桌面/交付相关 lane 追加�?
- `pnpm run tauri:build`

业务闭环与发布相�?lane 追加�?
- `pnpm run test`
- `pnpm run build:git-sdk`

## 6. 推荐 Lane 拆分

按写目录边界拆，而不是按概念拆：

| Lane | 负责 Step | 主写目录 | 禁改范围 |
| --- | --- | --- | --- |
| A | `02` | `packages/sdkwork-magic-studio-editor/` `packages/sdkwork-magic-studio-types/` | `assets` `src-tauri` `film` `magiccut` `canvas` |
| B | `03` | `packages/sdkwork-magic-studio-assets/` | `editor` `src-tauri` `release` |
| C | `04` | `packages/sdkwork-magic-studio-core/ai/` | `film` `magiccut` `canvas` 业务主干 |
| D | `05` | `src-tauri/` 安全与策略层 | 业务引擎目录 |
| E | `06` | `packages/sdkwork-magic-studio-film/` | `projectService.ts` `policy.rs` |
| F | `07` | `packages/sdkwork-magic-studio-magiccut/` | `projectService.ts` `policy.rs` |
| G | `08` | `packages/sdkwork-magic-studio-canvas/` | `projectService.ts` `policy.rs` |
| C2 | `09` | `chat` `prompt` 动作�?| 直接抢改 E/F/G 主干 |
| D2 | `10` | 插件运行时与沙箱目录 | 绕过 D 直接�?capability/policy |
| H | `11` | 发布、导出、安装、workflow | 在未移交前改 `projectService.ts` |
| I | `12` | `scripts/`、性能检查、观测相�?| 业务主干目录 |
| G0 | `00` `01` `13` | 总控文档、验收矩阵、合流记�?| 业务功能实现 |

## 7. 三周级压缩节�?
�?`8 agent` 标准模型或以上前提下，建议压缩为三周级别�?
| 周期 | 目标 |
| --- | --- |
| �?1 周前�?| 完成 `00` `01` |
| �?1 周后�?| 并行推进 `02-05`，冻结共享主�?|
| �?2 �?| 并行推进 `06-09`，完成核心创作闭�?|
| �?3 周前�?| 并行推进 `10-12`，完成插件、交付、性能治理 |
| �?3 周后�?| 完成 `13`，统一验收与发布裁�?|

前提�?
- 每个 wave 都必须有明确解锁条件
- 每个 lane 每天都必须回填验证和风险状�?- 任一 blocker 超时必须升级，不允许拖延并行

## 8. 波次解锁条件

### 8.1 进入 Wave B 的条�?
以下四项同时成立，才允许放开 `06-09`�?
1. `Step 02` 已冻�?`ProjectGraph` 与项目协�?2. `Step 03` 已冻�?`assetId` 与资产主�?3. `Step 04` 已冻�?AI Task 协议与主路径
4. `Step 05` 已冻�?capability 白名单与命令分级

### 8.2 进入 Wave C 的条�?
以下四项同时成立，才允许放开 `10-12`�?
1. Film 至少一条主链路可重复跑�?2. MagicCut 至少一条主链路可重复跑�?3. Canvas 至少一条主链路可重复跑�?4. Chat / Prompt 至少一条动作链路可重复跑�?
### 8.3 进入总验收的条件

以下三项同时成立，才允许进入 `13`�?
1. 插件最小运行时闭环成立
2. 真实交付链路成立
3. 性能预算门禁成立

## 9. 每日总控节奏

| 时间�?| 动作 | 负责�?|
| --- | --- | --- |
| `09:30` | 共享协议站会，只讨论冻结物与 blocker | 架构 owner |
| `11:30` | 共享文件锁与交接状态核�?| 集成 owner |
| `14:00` | blocker 升级窗口 | 架构 owner + 集成 owner |
| `16:30` | 合流窗口 | 集成 owner |
| `18:00` | lane 日结：验证、风险、例外、交接状�?| �?Lane owner |

## 10. 例外登记�?
总控允许存在临时例外，但必须登记，不允许口头例外�?
模板�?
```md
## 例外事项
## 影响 Step / Lane
## 批准�?## 失效时间
## 补偿验证
## 是否阻塞“解锁最�?bar�?## 是否阻塞“能力完�?bar�?```

审批权：

- 共享协议例外：架�?owner
- 权限与沙箱例外：安全 owner
- 性能预算例外：性能 owner
- 发布放行例外：发�?owner

未登记例外：

- 一律视为违�?- 一律阻塞完成�?
## 11. 必须立即停止并行的情�?
出现以下任一情况，必须立刻停止对应并�?lane，转入串行治理：

1. 两个 lane 需要同时主改同一个共享文件�?2. 共享协议发生变化，但消费方尚未重新确认�?3. 主写目录发生冲突�?4. 当前 lane 缺少 `docs/review/` 证据�?5. 当前 lane 没有证明包或交接包�?6. 当前 lane 为追求进度保留未登记 mock、fallback 或放宽权限�?
## 12. 快速完成的五条硬纪�?
1. 先冻结共享协议，再开放业务并行�?2. 先达到“解锁最�?bar”，再追求“能力完�?bar”�?3. 先完成本 lane 验证，再进入集成窗口�?4. 先处�?blocker 和例外，再继续扩范围�?5. 先回写架构与 review，再宣告能力兑现�?
## 13. 总控结论

真正能把周期压短的，不是“更多人同时改”，而是�?
- 有冻结的共享主干
- 有明确的 lane 写集边界
- 有统一�?preflight �?must-run 命令
- �?review、证明包、交接包三层证据
- 有清晰的例外审批与强停并行规�?
这也是为什�?`99` 必须被视�?program 级总控入口，而不是普通参考文档�?
