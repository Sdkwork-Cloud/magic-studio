> Migrated from `docs/release/2026-04-07-v0.1.39-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.39 迭代记录

## 本轮目标

继续执行 `/docs/prompts/反复执行Step指令.md`，对当前 workspace root 的离线恢复路径做真实实证。如果恢复失败，就把失败路径和回归收回到稳定状态，并给出明确的 No-Go 结论�?
## 当前 Step / Wave

- Current Step: `Step 03`
- Current Wave: `Wave A / 工具链阻塞项治理`

## 本轮完成�?
### 1. 完成离线 root install 路径实证

执行了：

- `pnpm.cmd install --offline --ignore-scripts --frozen-lockfile --filter . --reporter append-only`
- `$env:CI='true'; pnpm.cmd install --offline --ignore-scripts --frozen-lockfile --filter . --reporter append-only`

结论�?
- 第一条命令被 `pnpm` �?non-TTY 保护阻断
- 第二条命令在 `node_modules` 重建阶段超时
- 当前路径在本环境下不能作为安全默认恢复主路径

### 2. 收回离线恢复尝试引发的回�?
尝试后出现两个真实问题：

- `repair:deps` 新增 `vitest -> std-env` 缺口
- `test:vitest:safe` 回退到仓库根 `vite.config.ts`，重新触�?`tailwindcss/oxide` �?`spawn EPERM`

本轮已完成修复：

- 修复 `repair:deps` 对预发布 semver 范围的比较缺�?- 固定 `run-vitest-safe.mjs` 默认 `--config tests/vitest.codex.config.mjs`

### 3. 恢复当前稳定状�?
修复后重新验证得到：

- `actionableUniqueCount: 21`
- `frontierUniqueCount: 21`
- `std-env` 已退�?frontier
- `test:vitest:safe` 已恢复通过

## 代码变更范围

- `scripts/repair-workspace-node-modules.mjs`
- `scripts/run-vitest-safe.mjs`
- `scripts/__tests__/repair-workspace-node-modules.test.ts`
- `scripts/__tests__/run-vitest-safe.test.ts`

## 测试与验证结�?
### 失败路径验证

- `pnpm.cmd install --offline --ignore-scripts --frozen-lockfile --filter . --reporter append-only`
  - 结果：`ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`
- `$env:CI='true'; pnpm.cmd install --offline --ignore-scripts --frozen-lockfile --filter . --reporter append-only`
  - 结果：超�?
### 修复后定向验�?
- `node node_modules/vitest/vitest.mjs run --config tests/vitest.codex.config.mjs --configLoader native --pool threads --maxWorkers 1 --exclude ".worktrees/**" scripts/__tests__/run-vitest-safe.test.ts scripts/__tests__/repair-workspace-node-modules.test.ts tests/testRunnerBoundary.test.ts`
  - 结果：`3 files, 14 tests passed`
- `pnpm.cmd run test:vitest:safe -- scripts/__tests__/run-vitest-safe.test.ts scripts/__tests__/ensure-workspace-node-modules.test.ts scripts/__tests__/repair-workspace-node-modules.test.ts tests/testRunnerBoundary.test.ts packages/sdkwork-magic-studio-voicespeaker/src/components/VoiceLeftGeneratorPanel.boundary.test.ts packages/sdkwork-magic-studio-voicespeaker/src/components/voicespeaker/VoiceLabModal.boundary.test.ts packages/sdkwork-magic-studio-character/src/components/CharacterLeftGeneratorPanel.boundary.test.ts`
  - 结果：`7 files, 21 tests passed`
- `pnpm.cmd --silent run repair:deps`
  - 结果：`actionableUniqueCount=21`、`frontierUniqueCount=21`

## review 与架构回�?
- 新增 `docs/review/2026-04-07-step-03-offline-install-no-go-and-safe-runner-recovery.md`
- 更新 `docs/release/CHANGELOG.md`
- 更新 `docs/release/VERSION.md`
- 本轮无需更新 `docs/架构`

## 风险与回�?
### 风险

- 当前 `21` �?frontier 缺口仍然存在
- 再次直接执行 offline root install，可能再次破坏测试入�?
### 回滚

- 本轮所有改动都集中在脚本和测试�?- 如需回滚，可直接回退本轮脚本与测试改动，不影响业务模�?
## 对商业化交付的提�?
- 把“是否可以直接在当前 workspace root 做离线恢复安装”从猜测变成了实证结�?- 明确淘汰了一条当前环境下�?No-Go 恢复路径
- 收回了本轮尝试引入的测试入口回归，保持了 Step 03 的可持续推进能力

## 自我反证结论

- 没有把“做了恢复尝试”误判成“恢复成功�?- 没有把“frontier 回到 21”误判成“Step 03 完成�?- 没有把“修好了 safe runner”误判成“整库验证恢复�?- 当前仍只能说明：测试入口恢复了，但缺包主问题还在

## 下一轮计�?
1. 不再默认走当�?offline root install 路径�?2. 围绕 `21` �?frontier 缺口设计更小范围、更可控的恢复动作�?3. 恢复动作必须先定义：
   - 成功判据
   - 回滚动作
   - 影响范围
   - blocker 升级条件

