> Migrated from `docs/release/2026-04-07-v0.1.23-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.23 迭代记录

## 迭代主题

Step 03 第十九轮局部闭环：

- 继续复用 `ChooseAsset.projectReference` 主干能力
- 打�?`video-studio / LipSyncSection`
- 为源视频、源人像、驱动音频三个真实媒体入口补齐项目级 persisted reference 绑定

## 当前 Step / Wave

- 当前 Step: `Step 03`
- 当前 Wave: `Wave A / 共享主干收敛`
- 当前版本: `v0.1.23`
- Release Stage: `internal`

## 本轮交付

### 代码变更

- 更新 `packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.tsx`
  - �?`Upload Source Video` 增加 `slot: 'source-video'`
  - �?`Upload Portrait Image` 增加 `slot: 'source-image'`
  - �?`Upload Audio Driver` 增加 `slot: 'driver-audio'`
  - 三个槽位统一携带 `source: 'lip-sync-section'`

### 测试变更

- 新增 `packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.boundary.test.ts`
  - 固化三个真实媒体槽位已经接入 `projectReference` 的边界证�?
### 文档变更

- 新增 `docs/review/2026-04-07-step-03-lipsync-reference-binding.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 为什么做这一�?
�?`SubjectReferenceSection`、`StartEndFramesSection` 已收口之后，`LipSyncSection` 仍然保留三条高价值真实媒体输入路径：

- 源视�?- 源人�?- 驱动音频

如果这些输入仍然只是“可上传”而没有项目级 persisted reference�?
- 项目级引用审计会继续失真
- 删除保护无法完整覆盖 Lip Sync 创作链路
- 资产治理语义会在视频模式中继续碎片化

本轮目标是把这三条真实输入路径纳入统一治理协议，而不是重新发明一套局部方案�?
## 真实验证

### RED

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 test | 1 failed`
- 失败原因：当前源码没有声�?`projectReference`、`source-video`、`source-image`、`driver-audio`

### GREEN

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 file, 1 test passed`

### 回归

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/chooseAssetIdentity.test.tsx packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx packages/sdkwork-magic-studio-assets/tests/assetCenterProjectManagedImport.test.ts packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `7 files, 16 tests passed`

### Typecheck

命令�?
```bash
pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-assets/src/components/ChooseAsset.tsx packages/sdkwork-magic-studio-assets/src/components/chooseAssetProjectReference.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.tsx packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.tsx packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.tsx
```

结果�?
- `PASS`

## 版本结论

### 本轮结论

- `v0.1.23` 有效
- `internal` 持续通过
- `video-studio / LipSyncSection` 首次进入项目�?persisted reference 主干
- `Step 03` 继续进行中，未结�?
### 已达成能�?
- Lip Sync 所需源视频具备项目级引用语义
- Lip Sync 所需源人像具备项目级引用语义
- Lip Sync 所需驱动音频具备项目级引用语�?- 视频创作模式中的多种核心媒体输入开始共享同一 persisted reference 协议

### 尚未达成

- `VideoPromptStyleSection` �?`Optional Soundtrack` 仍未接入
- 视频包外�?`ChooseAsset` 消费面仍未全量审�?- 全仓 `test / typecheck / build` 尚未验证
- `alpha` 仍不能开�?
## 后续建议

1. 下一轮继续锁�?`Step 03`，优先处�?`VideoPromptStyleSection` 的音轨槽位�?2. 在视频包内完成最后一个真�?`ChooseAsset` 消费点后，再重新评估视频包维度的 persisted reference 收口状态�?3. 不要误报 `ChooseAsset` 全量完成，继续以真实入口和真实证据推进�?
## 商业化影响评�?
这一轮的价值很明确�?
- Lip Sync 链路首次具备项目级资产治理语�?- 项目级资产审计和删除保护不再遗漏关键音视频输�?- 视频创作模式正在从“分散上传器集合”转向“统一资产治理协议下的真实业务入口体系�?
