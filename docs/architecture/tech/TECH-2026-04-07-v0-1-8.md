> Migrated from `docs/release/2026-04-07-v0.1.8-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.8 迭代记录

## 本轮目标

继续执行 `docs/prompts/反复执行Step指令.md`，在 `Step 03` 内推进生成结�?identity 收敛的下一段真实闭环：

- 先解�?codex 专用 Vitest 子路�?alias blocker
- 再修�?`persistGeneratedSelectionAsset(...)` 对嵌�?`resource.metadata` canonical identity 的忽略问�?
## 当前 Step / Wave

- 当前 Step: `03-资产中心与本地存储闭环`
- 当前 Wave: `Wave A / 共享主干收敛`

## 本轮完成�?
- 更新 `tests/vitest.codex.config.mjs`
  - 补齐 workspace 包子路径 alias
  - �?`@sdkwork/magic-studio-core/services` 等子路径能在 codex 专用 Vitest 配置下解�?- 更新 `packages/sdkwork-magic-studio-assets/src/services/generatedSelectionAssetPersistence.ts`
  - 增加嵌套 identity 解析
  - 让持久化链路优先复用 canonical identity
- 更新 `packages/sdkwork-magic-studio-assets/tests/generatedSelectionAssetPersistence.test.ts`
  - 新增嵌套 `resource.metadata` identity 回归用例
- 新增 `docs/review/2026-04-07-step-03-generated-selection-persistence-nested-identity.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 代码变更范围

- `tests/vitest.codex.config.mjs`
- `packages/sdkwork-magic-studio-assets/src/services/generatedSelectionAssetPersistence.ts`
- `packages/sdkwork-magic-studio-assets/tests/generatedSelectionAssetPersistence.test.ts`

## 测试与验证结�?
### Red 证据一：基础设施

修复前执行：

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/generatedOutcomeAssetPersistence.test.ts packages/sdkwork-magic-studio-assets/tests/generatedSelectionAssetPersistence.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`

结果�?
- `2 failed`
- import 阶段直接报错：`Cannot find package '@sdkwork/magic-studio-core/services'`

### Red 证据二：业务行为

基础设施修复后，新增嵌套 identity 回归用例，再次执行：

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/generatedSelectionAssetPersistence.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`

结果�?
- `1 failed`
- 原因�?  - 未复用嵌�?canonical identity
  - 误入重新导入分支
  - �?mock 上传结果�?`metadata` 时抛异常

### Green / Regression

执行主题回归包：

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/generatedSelectionAsset.test.ts packages/sdkwork-magic-studio-assets/tests/generatedSelectionAssetPersistence.test.ts packages/sdkwork-magic-studio-assets/tests/generatedOutcomeAssetPersistence.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`

结果�?
- `3 files, 12 tests passed`

### 扩展验证尝试

�?`ChooseAsset` 测试一起纳入时，暴露出新的更深 blocker�?
- `retired generic app SDK` 子导�?`./http/client` 在当前条件下未导�?- `indexedDB is not defined`
- node 环境下触发了 `packages/sdkwork-magic-studio-core/src/platform/web.ts` �?web 平台初始�?
这说明当�?codex 自动化验证面已经推进到了更深层问题，而不是停留在 alias 级别�?
## 本轮未闭环项

- `ChooseAsset` 测试及其依赖�?node/web 平台隔离问题尚未处理
- `Film / MagicCut / Canvas` 导入链路的嵌�?identity 收敛尚未继续推进
- Step 03 仍未整体完成

## 风险与回�?
### 风险

- 本轮虽然修复�?codex 测试 alias 和持久化业务逻辑，但更大范围消费侧回归仍会遇到平台初始化问题
- 当前修复主要覆盖生成结果持久化入口，不代表所有生成结果相关链路都已统一

### 回滚

- 本轮改动可按文件级回滚：
  - `tests/vitest.codex.config.mjs`
  - `generatedSelectionAssetPersistence.ts`
  - `generatedSelectionAssetPersistence.test.ts`
- 不涉及数据库迁移、远程契约变化或桌面权限变更

## 对商业化交付的提�?
- 生成结果持久化链路开始真正以 canonical identity 为主，而不是把缺失顶层字段等价为“未持久化�?- 减少无意义的重新导入�?identity 漂移
- 提升 codex 自动化测试对共享资产主干的真实可观测�?
## 自我反证结论

- 本轮不是基础设施层空转，而是“基础设施清障 + 业务缺口闭环”同时完�?- 本轮不是 `Step 03` 完工，只是第四个局部闭�?- 下一轮仍应继续停留在 `Step 03`，优先在“导入链�?identity 收敛”和“node/web 测试隔离”之间做一次明确取�?
