> Migrated from `docs/release/2026-04-07-v0.1.20-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.20 迭代记录

## 迭代主题

Step 03 第十六轮局部闭环：

- 识别并拆穿一次“修在本�?mirror、但真实解析包未生效”的�?GREEN
- �?`sdkwork-magic-studio-core` �?focused asset-center wrapper 收敛�?`retired generic app SDK` 根导�?- 解除 Canvas 定向 `tsc --ignoreConfig` �?`retired generic app SDK subpath` unresolved subpath exports 的依�?
## 当前 Step / Wave

- 当前 Step: `Step 03`
- 当前 Wave: `Wave A / 共享主干收敛`
- 当前版本: `v0.1.20`
- Release Stage: `internal`

## 本轮交付

### 代码变更

- 更新 `packages/sdkwork-magic-studio-core/src/sdk/assetCenterClient.ts`
  - 停止直接导入 `retired generic app SDK subpath`、`retired generic app SDK subpath`、`retired generic app SDK subpath`
  - 改为�?`retired generic app SDK` 根导出统一导入 `createAssetCenterApi`、`createUploadApi`、`createHttpClient` 与相关类�?  - 保持 focused wrapper 语义，不回退�?`createClient(...)`

- 更新 `tests/assetsFocusedSdkClientBoundary.node.test.mjs`
  - �?shared wrapper 边界从“必须使�?subpath exports”调整为“必须使用真实可解析�?generated SDK root export�?  - 明确防止再次引入 unresolved `retired generic app SDK subpath` 直接依赖

### 文档变更

- 新增 `docs/review/2026-04-07-step-03-asset-center-sdk-root-export-convergence.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 为什么做这一�?
本轮不是简单把 `package.json.exports` 补一补�?
真实排查后确认：

- `pnpm-workspace.yaml` 排除了本�?`packages/sdkwork-app-sdk`
- `node_modules/retired generic app SDK` 实际�?Junction，指向外部共享源�?- 当前定向 `tsc --ignoreConfig` 与真实运行时解析都落在这个外部共享包
- 该包只导出根入口，没有导�?`./api/*`、`./http/*`、`./types/*`

因此，如果继续在本地 mirror 上打补丁，只会制造“文档和本地文件看起来修了，但真�?blocker 还在”的假闭环�?
本轮的真正价值，在于把问题从“误判目标文件”收敛到“真实解析链”，然后用应用侧 shared wrapper 的最小收敛方案解除当前主路径 blocker�?
## 真实验证

### RED

命令�?
```bash
node tests/assetsFocusedSdkClientBoundary.node.test.mjs
```

结果�?
- `7 tests | 1 failed`
- 失败点：
  - `Expected focused asset-center helper to consume the generated app SDK through the shared root export that actually ships in npm mode.`

同时，定�?`tsc` 失败�?
```bash
pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-canvas/src/utils/canvasReferenceImageImport.ts packages/sdkwork-magic-studio-canvas/src/utils/canvasReferenceImageAttachment.ts packages/sdkwork-magic-studio-canvas/src/components/CanvasNode.tsx
```

失败点：

- `Cannot find module 'retired generic app SDK subpath'`
- `Cannot find module 'retired generic app SDK subpath'`
- `Cannot find module 'retired generic app SDK subpath'`
- `Cannot find module 'retired generic app SDK subpath'`

### GREEN

命令�?
```bash
node tests/assetsFocusedSdkClientBoundary.node.test.mjs
```

结果�?
- `7 tests passed`

### Typecheck

命令�?
```bash
pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-canvas/src/utils/canvasReferenceImageImport.ts packages/sdkwork-magic-studio-canvas/src/utils/canvasReferenceImageAttachment.ts packages/sdkwork-magic-studio-canvas/src/components/CanvasNode.tsx
```

结果�?
- `PASS`

### 运行时根导出验证

命令�?
```bash
node --input-type=module -e "const sdk = await import('retired generic app SDK'); console.log(typeof sdk.createAssetCenterApi, typeof sdk.createUploadApi, typeof sdk.createHttpClient);"
```

结果�?
- 输出：`function function function`

### Step 03 资产中心回归

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/assetCenterProjectManagedImport.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `assetCenterProjectManagedImport.test.ts`: `7 tests passed`
- `assetCenterDeleteSafety.test.ts`: `3 tests passed`

## 版本结论

### 本轮结论

- `v0.1.20` 有效
- `internal` 持续通过
- Canvas 参考图主路径的定向 `tsc` blocker 已解�?- Step 03 继续推进，但仍未完成

### 达成的能�?
- `sdkwork-magic-studio-core` �?focused asset-center wrapper 已不再直接依赖当前共�?SDK 未开放的 subpath exports
- Magic Studio 当前主路径在 npm mode / node_modules Junction / `--ignoreConfig` 下恢复可解析
- 资产中心已有 Step 03 回归链路未被本轮 shared wrapper 收敛破坏

### 尚未达成

- `ChooseAsset` node/web �?persisted reference 收口仍未完成
- 外部共享 `retired generic app SDK` 契约源本身仍未补�?subpath exports
- 因此不能宣告 `Step 03` 完成，也不能进入 `alpha`

## 后续建议

1. 下一轮继续锁�?`Step 03`，优先审�?`ChooseAsset` node/web 真实入口�?`project-level persisted reference` 是否贯通�?2. 在当前仓库内继续坚持“feature -> focused wrapper -> shared app sdk root export”路径，不要新增新的 `retired generic app SDK subpath` 直接导入�?3. 将“共�?SDK 契约源补�?subpath exports”登记为跨仓共享治理项，待具备外部源码可写权限后做源头闭环�?
## 商业化影响评�?
这一轮虽然不是用户可见功能迭代，但它直接提升了商业化交付的工程稳定性：

- 降低了构建模式差异导致的隐性编译失败风�?- 降低�?shared wrapper 因解析链差异而在不同环境中表现不一致的风险
- 保住�?Step 03 已经积累出来的资产中心主干能力，不需要为同一�?shared SDK 解析问题反复返工

