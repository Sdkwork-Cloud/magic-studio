> Migrated from `docs/release/2026-04-07-v0.1.24-迭代记录.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 2026-04-07 v0.1.24 迭代记录

## 迭代主题

Step 03 第二十轮局部闭环：

- 继续复用 `ChooseAsset.projectReference` 主干能力
- 打�?`video-studio / VideoPromptStyleSection`
- �?`Optional Soundtrack` 音轨入口补齐项目�?persisted reference 绑定
- 完成视频包内直接 `ChooseAsset` 消费面的阶段性收�?
## 当前 Step / Wave

- 当前 Step: `Step 03`
- 当前 Wave: `Wave A / 共享主干收敛`
- 当前版本: `v0.1.24`
- Release Stage: `internal`

## 本轮交付

### 代码变更

- 更新 `packages/sdkwork-magic-studio-video/src/components/panel/VideoPromptStyleSection.tsx`
  - �?`Optional Soundtrack` 增加 `slot: 'audio-track'`
  - 增加 `source: 'video-prompt-style-section'`

### 测试变更

- 新增 `packages/sdkwork-magic-studio-video/src/components/panel/VideoPromptStyleSection.boundary.test.ts`
  - 固化音轨槽位已经接入 `projectReference` 的边界证�?
### 文档变更

- 新增 `docs/review/2026-04-07-step-03-video-prompt-style-soundtrack-binding.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 为什么做这一�?
在视频包中，前面几轮已经依次收口�?
- `SubjectReferenceSection`
- `StartEndFramesSection`
- `LipSyncSection`

�?`VideoPromptStyleSection` �?`Optional Soundtrack` 仍是一个真�?`ChooseAsset` 盲区�?
- 可以上传
- 但没有项目级 persisted reference
- 因而项目级资产治理、删除保护、引用审计仍然看不到这条音轨输入关系

本轮的价值在于让视频包内所有直�?`ChooseAsset` 消费点都进入统一治理协议，为后续把范围扩展到其他包奠定边界�?
## 真实验证

### RED

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-video/src/components/panel/VideoPromptStyleSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 test | 1 failed`
- 失败原因：当前源码没有声�?`projectReference`、`audio-track`、`video-prompt-style-section`

### GREEN

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-video/src/components/panel/VideoPromptStyleSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 file, 1 test passed`

### 回归

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/chooseAssetIdentity.test.tsx packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx packages/sdkwork-magic-studio-assets/tests/assetCenterProjectManagedImport.test.ts packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/panel/VideoPromptStyleSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `8 files, 17 tests passed`

### Typecheck

命令�?
```bash
pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-assets/src/components/ChooseAsset.tsx packages/sdkwork-magic-studio-assets/src/components/chooseAssetProjectReference.ts packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.tsx packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.tsx packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.tsx packages/sdkwork-magic-studio-video/src/components/panel/VideoPromptStyleSection.tsx
```

结果�?
- `PASS`

## 版本结论

### 本轮结论

- `v0.1.24` 有效
- `internal` 持续通过
- `video-studio / VideoPromptStyleSection` 首次进入项目�?persisted reference 主干
- 视频包内直接 `ChooseAsset` 消费面阶段性收口完�?- `Step 03` 继续进行中，未结�?
### 已达成能�?
- 视频模式中的主体参考图、起止帧、Lip Sync 媒体输入、可选音轨都已具备项目级引用语义
- 视频包内直接 `ChooseAsset` 消费点开始共享统一 persisted reference 协议
- 视频创作模式在资产治理层的语义完整度明显提升

### 尚未达成

- 视频包之外仍存在多个直接 `ChooseAsset` 消费点未收口
- `ChooseAssetModal` 消费面仍未审�?- 全仓 `test / typecheck / build` 尚未验证
- `alpha` 仍不能开�?
## 后续建议

1. 下一轮继续锁�?`Step 03`，扩展到视频包外的直�?`ChooseAsset` 消费点�?2. 优先级建议：
   - `VoicePersonaSection`
   - `CharacterLeftGeneratorPanel`
   - `MusicLeftGeneratorPanel`
3. �?`ChooseAssetModal` 收口作为独立 lane 处理，避免把范围混成不可验证的大任务�?
## 商业化影响评�?
这一轮的意义在于�?
- 视频包内真实媒体输入的项目级资产治理语义已经形成闭环
- 视频创作应用对输入资产的删除保护、引用审计、治理一致性有了更完整的基础
- 后续推进到音乐、角色、声音等包时，可以继续复用同一�?`projectReference` 协议，而不是重新复制视频包的收敛过�?
