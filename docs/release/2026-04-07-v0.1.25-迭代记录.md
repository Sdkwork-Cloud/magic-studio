# 2026-04-07 v0.1.25 迭代记录

## 迭代主题

Step 03 第二十一轮局部闭环：

- 从视频包扩展到视频包外的直接 `ChooseAsset` 消费�?- 打�?`voicespeaker / VoicePersonaSection`
- 为人设头像输入补齐项目级 persisted reference 绑定

## 当前 Step / Wave

- 当前 Step: `Step 03`
- 当前 Wave: `Wave A / 共享主干收敛`
- 当前版本: `v0.1.25`
- Release Stage: `internal`

## 本轮交付

### 代码变更

- 更新 `packages/sdkwork-magic-studio-voicespeaker/src/components/panel/VoicePersonaSection.tsx`
  - 为头像输入增�?`slot: 'persona-avatar'`
  - 增加 `source: 'voice-persona-section'`

### 测试变更

- 新增 `packages/sdkwork-magic-studio-voicespeaker/src/components/panel/VoicePersonaSection.boundary.test.ts`
  - 固化人设头像槽位已经接入 `projectReference` 的边界证�?
### 文档变更

- 新增 `docs/review/2026-04-07-step-03-voice-persona-avatar-binding.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 为什么做这一�?
视频包内的直�?`ChooseAsset` 消费点已经阶段性收口，下一步最优先的是向其他高价值业务包扩展。`VoicePersonaSection` 是当前最适合继续推进的入口：

- 只有一个真实素材输�?- 业务语义稳定
- 直接关系到声音人格的头像治理

如果这条路径仍然只是“可上传”而没有项目级 persisted reference�?
- 项目级资产治理会继续遗漏 voicespeaker 人设头像
- 删除保护与引用审计无法完整覆盖声音人格链�?
## 真实验证

### RED

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-voicespeaker/src/components/panel/VoicePersonaSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 test | 1 failed`
- 失败原因：当前源码没有声�?`projectReference`、`persona-avatar`、`voice-persona-section`

### GREEN

命令�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-voicespeaker/src/components/panel/VoicePersonaSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果�?
- `1 file, 1 test passed`

### 回归

命令 A�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/chooseAssetIdentity.test.tsx packages/sdkwork-magic-studio-assets/tests/chooseAssetProjectReference.test.tsx packages/sdkwork-magic-studio-assets/tests/assetCenterProjectManagedImport.test.ts packages/sdkwork-magic-studio-assets/tests/assetCenterDeleteSafety.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果 A�?
- `4 files, 13 tests passed`

命令 B�?
```bash
pnpm.cmd exec vitest run packages/sdkwork-magic-studio-video/src/components/modes/SubjectReferenceSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/modes/StartEndFramesSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/modes/LipSyncSection.boundary.test.ts packages/sdkwork-magic-studio-video/src/components/panel/VideoPromptStyleSection.boundary.test.ts packages/sdkwork-magic-studio-voicespeaker/src/components/panel/VoicePersonaSection.boundary.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"
```

结果 B�?
- `5 files, 5 tests passed`

补充�?
- `chooseAssetProjectReference.test.tsx` 单独重跑结果�?`2 tests passed`
- 说明一次合并命令中的沙箱路径波动不是功能回�?
### Typecheck

命令�?
```bash
pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-voicespeaker/src/components/panel/VoicePersonaSection.tsx
```

结果�?
- `FAIL`
- 阻塞来自�?  - `packages/sdkwork-magic-studio-voicespeaker/src/components/panel/types.ts`
  - `TS2307: Cannot find module '@sdkwork/magic-studio-generation-history'`

### 版本结论

- `v0.1.25` 有效
- `internal` 持续通过
- `voicespeaker / VoicePersonaSection` 首次进入项目�?persisted reference 主干
- `Step 03` 继续进行中，未结�?
## 已达成能�?
- 声音人格头像具备项目级引用语�?- voicespeaker 业务开始接入统一 `ChooseAsset.projectReference` 协议
- 跨包收敛已经从视频包扩展到声音人格业�?
## 尚未达成

- `CharacterLeftGeneratorPanel` 仍未接入
- `MusicLeftGeneratorPanel` 仍未接入
- `VoiceLabModal` 仍未接入
- `ChooseAssetModal` 消费面仍未审�?- 全仓 `test / typecheck / build` 尚未验证

## 后续建议

1. 下一轮继续锁�?`Step 03`，优先处�?`CharacterLeftGeneratorPanel`�?2. 之后处理 `MusicLeftGeneratorPanel` �?`sourceMusic` 入口�?3. �?voicespeaker 的类型依赖解析问题作为并行治理项单独处理，不阻塞 `projectReference` 主干收口�?