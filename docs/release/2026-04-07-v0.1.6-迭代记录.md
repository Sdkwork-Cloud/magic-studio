# 2026-04-07 v0.1.6 迭代记录

## 本轮目标

继续执行 `docs/prompts/反复执行Step指令.md`，在 `Step 03` 内选择一个共享主干断点做真实闭环�?
- 让统一资产 URL resolver 支持读取嵌套�?`resource / payload / primaryArtifact / delivery` 中的 canonical 资产引用
- 避免包装对象因为顶层没有 `assetId` 而退�?`null`、临�?URL 或裸路径

## 当前 Step / Wave

- 当前 Step: `03-资产中心与本地存储闭环`
- 当前 Wave: `Wave A / 共享主干收敛`

## 本轮完成�?
- 更新 `packages/sdkwork-magic-studio-assets/src/asset-center/application/assetUrlResolver.ts`
  - 新增受控嵌套资产载体收集逻辑
  - �?`getAssetIdCandidates(...)` 支持读取嵌套 `assetId / metadata.assetId`
  - �?`getDirectLocatorCandidates(...)` 支持读取嵌套 `path / url / src / uri / href`
  - 保持顶层 `id` 语义不变，避免嵌套任�?`id` 被误判成资产主键
- 新增 `packages/sdkwork-magic-studio-assets/tests/assetUrlResolver.test.ts`
  - 覆盖嵌套 `resource` 主引�?  - 覆盖嵌套 `payload.image` 托管 locator 回退
- 更新 `packages/sdkwork-magic-studio-magiccut/tests/magicCutStoreIdentity.test.tsx`
  - 补充一条与 canonical asset identity 对齐的回归保�?
## 代码变更范围

- `packages/sdkwork-magic-studio-assets/src/asset-center/application/assetUrlResolver.ts`
- `packages/sdkwork-magic-studio-assets/tests/assetUrlResolver.test.ts`
- `packages/sdkwork-magic-studio-magiccut/tests/magicCutStoreIdentity.test.tsx`

## 测试与验证结�?
### Red

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/assetUrlResolver.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`
  - 结果: `FAIL`
  - 证明�? 嵌套 `resource` �?`payload.image` 均无法被统一 resolver 解析

### Green / Regression

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/assetUrlResolver.test.ts packages/sdkwork-magic-studio-assets/tests/useAssetUrlDefaultResolver.test.ts packages/sdkwork-magic-studio-magiccut/tests/magicCutStoreIdentity.test.tsx --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`
  - 结果: `3 files, 8 tests passed`

### Typecheck Attempt

- `pnpm.cmd exec tsc --skipLibCheck --ignoreConfig --noEmit --jsx react-jsx --module ESNext --moduleResolution bundler --target ES2022 --lib ES2022,DOM packages/sdkwork-magic-studio-assets/src/asset-center/application/assetUrlResolver.ts`
  - 结果: `FAIL`
  - 原因: 仓库当前缺少 `retired generic app SDK subpath` 相关类型声明，阻塞在 `packages/sdkwork-magic-studio-core/src/sdk/assetCenterClient.ts`

## review 与架构回�?
- 新增 `docs/review/2026-04-07-step-03-asset-url-resolver-nested-reference.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`
- 本轮未更�?`docs/架构/`
  - 原因: 本轮是既�?Step 03 能力落地，不涉及架构标准增删

## 风险与回�?
### 风险

- Step 03 仍未完成跨引擎全量主引用切换
- 生成结果、工程快照、删除回收与引用追踪仍需继续收敛
- 全仓 `tsc` 仍被共享 SDK 类型缺口阻塞

### 回滚

- 本轮改动集中在共�?resolver 与定向测�?- 若需回滚，可按文件级回滚�?  - `assetUrlResolver.ts`
  - `assetUrlResolver.test.ts`
  - `magicCutStoreIdentity.test.tsx`
- 不涉及数据库迁移、远程契约变更或桌面权限回滚

## 对商业化交付的提�?
- 统一包装对象�?canonical 资产实体之间的解析桥�?- 降低临时 URL、裸路径�?UI 与引擎中的主引用地位
- 为后�?Step 03 的跨引擎资产接线和删除安全策略提供更稳的 identity 主干

## 自我反证结论

- 本轮不是文档层补丁，而是共享主干行为修正
- 本轮不是 Step 03 完工，只是第二个真实闭环
- 下一轮仍应继续留�?`Step 03`，优先完成：
  1. 引擎内部直接 URL 主引用收�?  2. `assetId / primaryResourceId / resourceViewId` 写回协议统一
  3. 删除前反向引用追踪与回收治理
