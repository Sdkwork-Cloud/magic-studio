# 2026-04-07 v0.1.7 迭代记录

## 本轮目标

继续执行 `docs/prompts/反复执行Step指令.md`，在 `Step 03` 内选择一个真实、可验证、共享价值高的收口点�?
- 修复 AI 生成结果在共享资产投影阶段无法识别嵌�?canonical identity 的问�?- 避免顶层字段缺失时，资产投影退�?`selection.key / artifact uuid / 临时 url`

## 当前 Step / Wave

- 当前 Step: `03-资产中心与本地存储闭环`
- 当前 Wave: `Wave A / 共享主干收敛`

## 本轮完成�?
- 更新 `packages/sdkwork-magic-studio-assets/src/components/generate/generatedSelectionAsset.ts`
  - 新增嵌套 identity 解析逻辑
  - 支持�?`resource` 及其 `metadata` 中读�?canonical identity
  - 同步修正 `id / uuid / metadata` 的投影结�?- 更新 `packages/sdkwork-magic-studio-assets/tests/generatedSelectionAsset.test.ts`
  - 新增嵌套 `resource.metadata` 身份回归用例
- 新增 `docs/review/2026-04-07-step-03-generated-selection-nested-identity.md`
- 更新 `docs/release/VERSION.md`
- 更新 `docs/release/CHANGELOG.md`

## 代码变更范围

- `packages/sdkwork-magic-studio-assets/src/components/generate/generatedSelectionAsset.ts`
- `packages/sdkwork-magic-studio-assets/tests/generatedSelectionAsset.test.ts`

## 测试与验证结�?
### Red 证据

新增嵌套身份回归用例后，修复前实际失败表现为�?
- 输入已在 `resource.metadata` 中携带：
  - `assetId`
  - `assetUuid`
  - `primaryResourceId`
  - `primaryResourceUuid`
  - `resourceViewId`
  - `resourceViewUuid`
- 但输出仍然退化为�?  - `id = selection.key`
  - `uuid = artifact uuid`
  - metadata canonical identity 丢失

这证明共享投影主干在修复前确实无法承接嵌�?canonical identity�?
### Green / Regression

- `pnpm.cmd exec vitest run packages/sdkwork-magic-studio-assets/tests/generatedSelectionAsset.test.ts --config tests/vitest.codex.config.mjs --configLoader native --pool threads --exclude ".worktrees/**"`
  - 结果：`1 file, 3 tests passed`

## 本轮未闭环项

- `persistGeneratedSelectionAsset(...)` 同类问题尚未在本轮实现�?- 继续扩展到消费侧/持久化侧验证时，碰到当前仓库已有�?`@sdkwork/magic-studio-core/services` 模块解析 blocker�?- `Film / MagicCut / Canvas` 导入链路中的嵌套身份收敛还未继续推进�?
## 风险与回�?
### 风险

- 本轮只修复共享转换入口，不代表生成结果全链路都已经统一 identity�?- 仓库当前测试基础设施仍存在局部模块解析问题，会影响下一轮大范围回归�?
### 回滚

- 本轮改动集中且可按文件级回滚�?  - `generatedSelectionAsset.ts`
  - `generatedSelectionAsset.test.ts`
- 不涉及数据库迁移、远程契约变更或桌面权限回滚�?
## 对商业化交付的提�?
- 共享资产入口开始真正以 canonical identity 为主，而不是以临时生成结果为主�?- 降低 AI 生成结果进入资产中心、工程快照、后续编辑链路时的身份漂移风险�?- 为下一轮持久化协议统一和引擎导入收敛打下更稳的基线�?
## 自我反证结论

- 本轮是共享能力修复，不是单页补丁�?- 本轮不是 `Step 03` 完工，只是第三个局部闭环�?- 下一轮仍应继续停留在 `Step 03`，优先把同类嵌套身份缺口推进到持久化和引擎导入链路�?