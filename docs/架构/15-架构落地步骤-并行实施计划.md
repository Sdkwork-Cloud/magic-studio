# Magic Studio V2 Architecture Implementation Plan

> Historical execution plan only. This document may reference retired paths and should not be used as the current architecture standard.
>
> Current standards:
> - `docs/magic-studio-unified-host-api-standard.md`
> - `docs/tauri-rust-framework-architecture.md`
> - `docs/platform-runtime-capability-matrix.md`

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）�?`superpowers:executing-plans` �?step 执行。所�?step 都必须按“完成定�?+ 验证命令 + 合并门禁”执行，禁止只完成代码、不完成验收�?
**Goal:** 将本目录架构文档收敛为一套可落地、可验收、可并行实施的执行步骤，确保每个 step 完成后都形成一个完整能力切片，而不是半成品�?
**Architecture:** 采用“基线门�?-> 共享主干 -> 三大引擎闭环 -> 生态与发布 -> 集成验收”五层推进方式。共享主干先收敛项目、资产、AI、桌面安全，再让 `film`、`magic-cut`、`canvas` 在稳定底座上并行深化，最后统一进入插件、发布、性能和整体联调�?
**Tech Stack:** `React`、`TypeScript`、`Vite`、`pnpm workspace`、`Turbo`、`Tauri 2`、`Rust`、`retired generic app SDK`、`Vitest`、GitHub Actions�?
---

## 1. 对现�?step 的复查结�?
当前 [14-行业对标-评估标准与演进路�?md](./14-行业对标-评估标准与演进路�?md) 中的阶段划分已经具备方向正确性，但还不算“完�?step”，主要�?6 个要件：

1. 没有精确到现有代码文件和包的落点�?2. 没有明确“做到什么程度才算这�?step 完成”�?3. 没有说明每个 step 完成后具体闭环了哪条产品能力�?4. 没有写清楚前置依赖和可并行关系�?5. 没有把验证命令和门禁标准钉死�?6. 没有针对多人并行开发给出写集隔离和合流策略�?
因此本文件的目标不是重写架构，而是把架构文档变成工程执行文档�?
## 2. 完美 step 的判定标�?
一�?step 只有同时满足以下条件，才允许标记完成�?
- 能力闭环：完成后，用户可实际使用一条完整能力链路，而不是只能看到界面或数据结构�?- 写集清晰：必须能明确主要修改文件与模块归属�?- 验收明确：必须有可执行验证命令、预期结果和回归范围�?- 依赖清晰：必须知道它依赖谁、谁依赖它�?- 可合并：完成后可以独立合并进主干，不留下破坏性中间态�?- 可并行：若允许并行，必须具备与其�?step 的清晰边界�?
## 3. 全局执行规则

- 所�?step 默认要求补齐自动化测试或契约校验，不接受“人工点过了”作为完成标准�?- 任何 step 都不得继续扩散旁路实现，尤其是手写业�?HTTP、直接散�?Tauri invoke、绕过资产中心的文件落地�?- 所有大改动都先在各自边界内完成，再进入集成分支，不允许多个 workstream 先在主干上交叉试错�?- 任何 step 如果会修改共享主干文件，必须先冻结协议，再让下游并行�?
## 4. 全局依赖�?
```text
Step 0 基线门禁
  -> Step 1 工程图谱
  -> Step 2 资产与存�?  -> Step 3 AI 主干
  -> Step 4 桌面安全与运行时

Step 1 + Step 2 + Step 3
  -> Step 5 Film 闭环
  -> Step 6 MagicCut 闭环
  -> Step 7 Canvas 闭环
  -> Step 8 Chat/Prompt 动作�?
Step 4 + Step 3 + Step 1
  -> Step 9 插件运行�?  -> Step 10 发布与导出闭�?  -> Step 11 性能与观测治�?
Step 5 + Step 6 + Step 7 + Step 8 + Step 9 + Step 10 + Step 11
  -> Step 12 集成联调与正式门�?```

## 4.1 架构章节�?Step 的闭环映�?
下表用于防止“step 做完了，但架构文档里的能力还只是口头完成”。任�?step 完成后，都必须能回指到对应章节，并给出实现证据�?
| 架构文档 | 必须由哪�?step 落地 | step 完成后必须能证明的能�?|
| --- | --- | --- |
| `01-产品设计与需求范围` | `Step 1`、`Step 5`、`Step 6`、`Step 7`、`Step 8`、`Step 10`、`Step 12` | 主链路从创作入口到导�?/ 发布有真实可执行路径，而不是能力清单堆�?|
| `02-架构标准与总体设计` | `Step 0`、`Step 1`、`Step 3`、`Step 4`、`Step 11`、`Step 12` | 单一主干、统一门禁、统一治理成立，不再长期并存多套平行实�?|
| `03-功能矩阵与核心流程` | `Step 5`、`Step 6`、`Step 7`、`Step 8`、`Step 10`、`Step 12` | Film / MagicCut / Canvas / Chat-Prompt 主流程都能按矩阵逐条对上 |
| `04-模块规划与边界设计` | `Step 1`、`Step 2`、`Step 3`、`Step 4`、`Step 9`、`Step 12` | 项目、资产、AI、桌面、插件的模块边界清晰，主域服务不再互相绕�?|
| `05-数据模型与资�?工程存储设计` | `Step 1`、`Step 2`、`Step 5`、`Step 6`、`Step 7`、`Step 10`、`Step 12` | 项目图谱、资产模型、导出物、发布物都有统一引用和可追踪身份 |
| `06-前端架构与组件化设计` | `Step 5`、`Step 6`、`Step 7`、`Step 8`、`Step 11`、`Step 12` | 各模�?UI 不只是能展示，而是建立在可维护状态模型、动作协议和性能约束之上 |
| `07-桌面运行时与本地能力架构` | `Step 4`、`Step 9`、`Step 10`、`Step 12` | Tauri 本地能力具备最小权限、策略治理、沙箱边界和交付链路 |
| `08-AI接入与多模态生成架构` | `Step 3`、`Step 5`、`Step 7`、`Step 8`、`Step 12` | AI 入口统一、任务统一、结果统一、动作统一，不再让业务层各自接模型 |
| `09-canvas-film-magiccut创作引擎设计` | `Step 5`、`Step 6`、`Step 7`、`Step 8`、`Step 12` | 三大引擎都形成完整可生产闭环，并且能通过统一主干互�?|
| `10-技术选型与工程治理` | `Step 0`、`Step 1`、`Step 3`、`Step 11`、`Step 12` | 技术选型不是静态说明，而是转化为脚本、目录、验证命令和门禁 |
| `11-性能架构与优化标准` | `Step 11`、`Step 12` | 性能进入预算化治理，关键页面和任务具备可观测与阻断标�?|
| `12-安全架构与治理标准` | `Step 4`、`Step 9`、`Step 10`、`Step 12` | 权限、命令、插件、发布全部进入最小权限与风险控制框架 |
| `13-测试质量-安装部署-发布运维` | `Step 0`、`Step 10`、`Step 12` | 类型、测试、构建、制品、安装、发布、回滚形成真实交付路�?|
| `14-行业对标-评估标准与演进路线` | `Step 0`、`Step 11`、`Step 12` | 对标不再停留在愿景，能够用评分、门禁和就绪清单证明演进结果 |

## 4.2 Step 输入 / 输出 / 退出信号总表

下表是执行级约束。每�?step 结束时必须同时满足“输出产物齐全”和“退出信号成立”，否则只能算进行中，不能算完成�?
| Step | 必须输入 | 必须输出 | 不允许结束的状�?|
| --- | --- | --- | --- |
| `Step 0` | 当前仓库真实状态、现有脚本、已知短�?| 基线报告、统一命令集、禁止事项、风险台�?| 没有统一门禁；后�?lane 对真实起点认知不一�?|
| `Step 1` | 基线门禁、现有项目服务、三引擎项目模型 | `ProjectGraph` 模型、服务、测试、三引擎绑定结果 | 仍存在新增私有项目主模型；`projectService` 继续承担 mock 主路�?|
| `Step 2` | 基线门禁、现有目录结构、各模式资源引用 | 统一资产身份、目录布局、引用关系、资产测�?| 任一模式仍以裸路径作为主引用；资产无法反向追�?|
| `Step 3` | 基线门禁、当�?AI 双路径、现有生成入�?| 统一任务协议、主路径策略、任务中心、结果归�?| 业务层仍可绕过任务中心直接成为默认主路径；结果不进资产中�?|
| `Step 4` | 基线门禁、当�?Tauri 能力配置、风险命令清�?| 路径白名单、命令分级、策略层测试、能力矩阵报�?| 仍保留全局放开权限；高风险命令不经过策略校�?|
| `Step 5` | `ProjectGraph`、资产中心、AI 任务中心 | Film 结构化项目、角色场景镜头资产化、预览闭�?| Film 仍只是页面集合；生成结果不能回到工程与资产主�?|
| `Step 6` | `ProjectGraph`、资产中心、桌面导出能�?| 时间线资产统一、真实导出、工程恢复、模板闭�?| 时间线继续持有旁路资源引用；导出仍是临时链路 |
| `Step 7` | `ProjectGraph`、资产中心、AI 任务中心 | 节点协议、执行结果归档、历史恢复、向下游输出 | Canvas 只能本地执行演示，不能把结果送入项目主干或下游引�?|
| `Step 8` | AI 动作协议、三引擎最小动作面 | 动作解析器、真实动作执行、跨模式动作验证 | Chat / Prompt 仍只输出文本建议；三引擎动作格式继续分裂 |
| `Step 9` | 权限白名单、策略层、插件目录规�?| Manifest、权限申请、沙箱目录、生命周期服�?| 插件可运行但无权限校验；插件与桌面安全脱�?|
| `Step 10` | 项目图谱、桌面能力、现有发布链�?| 同步服务、发布服务、导出一致性、发布脚本说�?| `projectService` 仍保�?mock 交付主路径；制品链路与项目导出脱�?|
| `Step 11` | 基线门禁、关键页面与任务链路、构建脚�?| 预算矩阵、观测点、预算检查脚本、性能报告 | 只有口头性能目标；没有脚本化预算和阻断标�?|
| `Step 12` | `Step 5` �?`Step 11` 的全部产物与验证结果 | E2E 验收矩阵、发布就绪清单、Go/No-Go 结论 | 仍有核心链路未验证；仍靠“应该可以”来声明架构完成 |

## 4.3 每个 Step 的强制完工证�?
所�?step 除了各自�?`Run / Expected / 完成定义` 之外，还必须附带以下证据，缺任一项都不得标记完成�?
1. 变更文件清单，且写集归属明确，能回答“谁拥有这个目录、谁 review、谁集成”�?2. �?step 对应的自动化验证输出，至少覆盖类型检查、单�?/ 契约测试、必要时的构建或桌面产物验证�?3. 对应架构章节的映射说明，能指出“本 step 实现了哪几章、以什么证据证明”�?4. 输入 / 输出工件清单，能明确交接给下�?step 的产物是什么�?5. mock、fallback、兼容层的处理说明，能区分“已下线”“受控保留”“暂缓移除但不再主路径”�?6. 风险说明与回滚方案，至少覆盖数据迁移、权限收紧、导出兼容、旧工程兼容�?7. 演示脚本或验收路径，能让 QA 或架�?reviewer 不读代码也能复现主链路�?8. 合并门禁记录，能证明�?step 可以独立并回主干而不留下破坏性中间态�?
## 5. 实施步骤

### Step 0: 基线冻结与门禁先�?
**目标能力闭环�?*
完成后，团队拥有统一的执行基线、验证门禁和风险登记，后续所有并行开发都基于同一套事实，不再一边改架构一边改变目标�?
**Files:**
- Modify: `package.json`
- Modify: `docs/架构/13-测试质量-安装部署-发布运维.md`
- Modify: `docs/架构/15-架构落地步骤-并行实施计划.md`
- Modify: `scripts/validate-service-encapsulation-policy.mjs`
- Create: `docs/reports/magic-studio-architecture-baseline.md`

- [ ] **Step 0.1: 固化现有验证命令集合**

输出统一基线命令，至少覆盖：

```bash
pnpm typecheck
pnpm test
pnpm audit:services:policy
pnpm verify:sdk-mode-parity
pnpm verify:mode-style-parity
```

- [ ] **Step 0.2: 记录当前已知风险和禁止事�?*

必须在基线报告中明确记录�?
- `projectService` 仍为 mock
- `pluginsBusinessService` 仍为空适配�?- `src-tauri/capabilities/default.json` 权限过宽
- AI 仍存�?backend SDK 与直连模型双路径

- [ ] **Step 0.3: 把架构门禁前置到所有后�?step**

要求所�?step 都默认检查：

- 是否新增旁路 API
- 是否绕过资产中心
- 是否扩大本地权限
- 是否引入不可测试的隐式状�?
- [ ] **Step 0.4: 运行基线验证**

Run:

```bash
pnpm typecheck
pnpm test
pnpm audit:services:policy
```

Expected:

- 类型检查通过
- 当前测试集通过
- 服务封装策略无新增违�?
**完成定义�?*

- 后续所�?step 均引用这套门�?- 风险清单与架构短板形成单一事实来源
- 团队对“当前真实起点”无歧义

**前置依赖�?* �?
**可并行：**

- 不可并行。它是所有后续工作的统一起点�?
---

### Step 1: 统一项目图谱与工程协�?
**目标能力闭环�?*
完成后，`workspace`、`project`、`film`、`magic-cut`、`canvas` 拥有统一项目上下文和稳定引用主干，后续引擎开发不再各自发明项目模型�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-workspace/src/services/workspaceService.ts`
- Modify: `packages/sdkwork-magic-studio-editor/src/services/projectService.ts`
- Modify: `packages/sdkwork-magic-studio-editor/src/services/index.ts`
- Create: `packages/sdkwork-magic-studio-editor/src/entities/projectGraph.entity.ts`
- Create: `packages/sdkwork-magic-studio-editor/src/services/projectGraphService.ts`
- Create: `packages/sdkwork-magic-studio-editor/src/services/projectGraphService.test.ts`
- Modify: `packages/sdkwork-magic-studio-film/src/services/filmProjectService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/magicCutProjectService.ts`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasService.ts`

- [ ] **Step 1.1: 定义统一 Project Graph**

至少包含�?
- `projectId`
- `workspaceId`
- `projectType`
- `engineDocumentRef`
- `assetRefs`
- `generationExecutionRefs`
- `exportRefs`
- `publishRefs`

- [ ] **Step 1.2: 去掉 editor 项目服务中的 mock 路径依赖**

要求 `projectService` 不再承担 mock �?Git / publish 伪实现，而是只负责工程域协议或显式委托给真实实现层�?
- [ ] **Step 1.3: 三大引擎接入统一项目图谱**

要求�?
- `Film` 打开项目时读取统一 `ProjectGraph`
- `MagicCut` 时间线工程绑�?`ProjectGraph`
- `Canvas` 画布工程绑定 `ProjectGraph`

- [ ] **Step 1.4: 补齐项目图谱测试**

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-film/src/services/filmProjectService.test.ts
pnpm exec vitest run packages/sdkwork-magic-studio-editor/src/services/projectGraphService.test.ts
pnpm typecheck
```

Expected:

- 项目图谱创建、读取、绑定、迁移通过测试
- 三大引擎对工程上下文的读取类型一�?
**完成定义�?*

- 工程上下文成为单一事实来源
- 后续所有引�?feature 都挂到同一 `ProjectGraph`
- 不再允许新增引擎私有项目主模�?
**前置依赖�?* `Step 0`

**可并行：**

- 可与 `Step 2`、`Step 3`、`Step 4` 并行
- 不可与任何会同时�?`projectService.ts` 的任务并�?
---

### Step 2: 资产中心与本地存储闭�?
**目标能力闭环�?*
完成后，所有导入、生成、剪辑、影视、画布结果都以统一资产模型落地，并且具备本地优先、引用可追踪、存储策略可治理的完整闭环�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-assets/src/asset-center/application/AssetCenterService.ts`
- Modify: `packages/sdkwork-magic-studio-assets/src/asset-center/application/magicStudioAssetLayout.ts`
- Modify: `packages/sdkwork-magic-studio-assets/src/asset-center/domain/assetCenter.domain.ts`
- Modify: `packages/sdkwork-magic-studio-settings/src/constants.ts`
- Modify: `packages/sdkwork-magic-studio-settings/src/entities/settings.entity.ts`
- Modify: `packages/sdkwork-magic-studio-film/src/utils/filmAssetIdentity.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/AssetCacheService.ts`
- Modify: `packages/sdkwork-magic-studio-canvas/src/utils/canvasIdentity.ts`
- Create: `packages/sdkwork-magic-studio-assets/src/asset-center/application/AssetCenterService.test.ts`

- [ ] **Step 2.1: 统一资产引用规范**

所有引擎和业务必须保存�?
- `assetId`
- 必要�?render metadata

禁止继续保存�?
- 仅裸路径
- 无法反向追踪的临�?URL

- [ ] **Step 2.2: 固化本地目录与存储层�?*

统一�?
- `workspaces`
- `projects`
- `assets`
- `cache`
- `exports`
- `logs`

- [ ] **Step 2.3: 补齐资产引用关系与版本信�?*

要求资产能追踪：

- 来源
- 所属项�?- 所属模�?- 派生产物
- 引用计数或反向引�?
- [ ] **Step 2.4: 验证多模式资产闭�?*

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-assets/src/asset-center/application/AssetCenterService.test.ts
pnpm exec vitest run packages/sdkwork-magic-studio-canvas/src/services/canvasService.test.ts
pnpm exec vitest run packages/sdkwork-magic-studio-film/src/services/filmService.test.ts
pnpm typecheck
```

Expected:

- 图像 / 视频 / 音频导入可归�?- `film`、`magic-cut`、`canvas` 均能�?`assetId` 读取资产

**完成定义�?*

- 资产中心成为所有创作结果的唯一落地主干
- 本地优先目录标准固定
- 后续插件、发布、导出都能复用同一资产模型

**前置依赖�?* `Step 0`

**可并行：**

- 可与 `Step 1`、`Step 3`、`Step 4` 并行
- `Step 5`、`Step 6`、`Step 7` 开始前必须完成

---

### Step 3: AI Gateway 与统一任务中心收敛

**目标能力闭环�?*
完成后，系统具备统一�?AI 任务协议、统一的模型接入主路径、统一的结果归档路径，直连模型只能作为受控 fallback，而不是平行主架构�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-core/src/ai/genAIService.ts`
- Modify: `packages/sdkwork-magic-studio-core/src/sdk/useAppSdkClient.ts`
- Modify: `packages/sdkwork-magic-studio-video/src/services/videoService.ts`
- Modify: `packages/sdkwork-magic-studio-prompt/src/services/promptBusinessService.ts`
- Modify: `packages/sdkwork-magic-studio-chat/src/services/chatService.ts`
- Create: `packages/sdkwork-magic-studio-core/src/ai/aiTaskCenter.ts`
- Create: `packages/sdkwork-magic-studio-core/src/ai/aiTaskCenter.test.ts`
- Create: `packages/sdkwork-magic-studio-core/src/ai/aiGatewayPolicy.ts`

- [ ] **Step 3.1: 定义统一 AI Task 协议**

统一字段至少包含�?
- `taskId`
- `scene`
- `model`
- `provider`
- `status`
- `progress`
- `inputRefs`
- `outputAssetRefs`
- `cost`
- `errorCode`

- [ ] **Step 3.2: 将后�?SDK 确认为主路径**

要求�?
- 生产路径默认先走 backend SDK
- 直连模型必须有明确的 fallback 标记和配置开�?- 直连模型不可再默认为普通业务主路径

- [ ] **Step 3.3: 统一 Chat / Prompt / Video 的任务记录与结果落库**

要求�?
- 任务完成后结果自动进入资产中�?- 对话与提示词增强结果可追踪来源和模型

- [ ] **Step 3.4: 验证 AI 主干一致�?*

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-core/src/ai/aiTaskCenter.test.ts
pnpm exec vitest run packages/sdkwork-magic-studio-film/src/services/filmService.test.ts
pnpm exec vitest run packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.test.ts
pnpm typecheck
```

Expected:

- 三类调用入口共用统一任务协议
- 输出结果可归档、可追踪、可取消

**完成定义�?*

- AI 架构从“双主干”收敛为“一主一路受�?fallback�?- 所有生成结果具备统一任务 ID 与资产引�?- 后续引擎嵌入 AI 时不再重复造轮�?
**前置依赖�?* `Step 0`

**可并行：**

- 可与 `Step 1`、`Step 2`、`Step 4` 并行
- `Step 5`、`Step 6`、`Step 7`、`Step 8` 开始前必须冻结协议

---

### Step 4: 桌面权限收敛与运行时治理

**目标能力闭环�?*
完成后，桌面端具备工作区级路径白名单、最小权限能力配置和高风险命令治理，系统可以在保留本地强能力的同时满足专业桌面应用的基本安全要求�?
**Files:**
- Modify: `src-tauri/capabilities/default.json`
- Modify: `src-tauri/src/framework/services/policy.rs`
- Modify: `src-tauri/src/framework/services/filesystem.rs`
- Modify: `src-tauri/src/framework/services/media.rs`
- Modify: `src-tauri/src/framework/services/jobs.rs`
- Modify: `src-tauri/src/framework/services/toolkit.rs`
- Create: `src-tauri/src/framework/services/policy.test.rs`
- Create: `docs/reports/desktop-capability-scope-matrix.md`

- [ ] **Step 4.1: 移除全局 `fs:scope = **`**

改为工作区相关白名单路径�?
- workspace root
- assets root
- cache root
- exports root
- plugin sandbox root

- [ ] **Step 4.2: 建立命令风险分级**

至少区分�?
- 读信�?- 工作区内读写
- 删除 / 重命�?- 媒体执行
- 外部命令 / shell

- [ ] **Step 4.3: 让高风险任务全部�?policy 校验**

包括�?
- 文件删除
- 媒体命令执行
- 插件未来的本地调�?
- [ ] **Step 4.4: 运行桌面能力校验**

Run:

```bash
pnpm tauri:build
pnpm verify:release:artifacts
pnpm typecheck
```

Expected:

- 桌面构建可通过
- 权限配置不再是全局放开
- 关键能力仍能在工作区内使�?
**完成定义�?*

- 桌面默认权限达到最小化
- 本地强能力全部纳入策略层
- 插件运行时的安全基础准备完毕

**前置依赖�?* `Step 0`

**可并行：**

- 可与 `Step 1`、`Step 2`、`Step 3` 并行
- `Step 9`、`Step 10` 开始前必须完成

---

### Step 5: Film 引擎闭环

**目标能力闭环�?*
完成后，`Film` 能从脚本 / 项目入口进入，完整管理角色、场景、道具、分镜、生成任务和预览，并且所有结果归入统一工程与资产体系�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-film/src/pages/FilmHomePage.tsx`
- Modify: `packages/sdkwork-magic-studio-film/src/pages/FilmEditorPage.tsx`
- Modify: `packages/sdkwork-magic-studio-film/src/services/filmService.ts`
- Modify: `packages/sdkwork-magic-studio-film/src/services/filmProjectService.ts`
- Modify: `packages/sdkwork-magic-studio-film/src/services/filmBusinessService.ts`
- Modify: `packages/sdkwork-magic-studio-film/src/store/filmStore.tsx`
- Modify: `packages/sdkwork-magic-studio-film/src/entities/film.entity.ts`
- Modify: `packages/sdkwork-magic-studio-film/src/utils/filmAssetIdentity.ts`

- [ ] **Step 5.1: �?Film 全量接入统一项目图谱**

- [ ] **Step 5.2: 让角�?/ 场景 / 道具 / 分镜全部资产化或项目�?*

- [ ] **Step 5.3: �?Film 中的 AI 任务全部走统一任务中心**

- [ ] **Step 5.4: 形成“脚�?-> 分镜 -> 生成 -> 预览”闭�?*

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-film/src/services/filmService.test.ts
pnpm exec vitest run packages/sdkwork-magic-studio-film/src/services/filmProjectService.test.ts
pnpm typecheck
```

Expected:

- Film 项目创建、打开、保存、生成、引用资产均可用

**完成定义�?*

- `Film` 不再只是�?tab 页面，而是完整结构化影视创作引�?- 与资产中心、AI 中心、项目图谱全部打�?
**前置依赖�?* `Step 1`、`Step 2`、`Step 3`

**可并行：**

- 可与 `Step 6`、`Step 7`、`Step 8` 并行
- 推荐独立 owner，仅持有 `packages/sdkwork-magic-studio-film/*` 写集

---

### Step 6: MagicCut 引擎闭环

**目标能力闭环�?*
完成后，`MagicCut` 具备素材导入、时间线编辑、播放器预览、属性修改、模板保存和真实导出能力的完整生产闭环�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-magiccut/src/pages/MagicCutPage.tsx`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/components/MagicCutEditor.tsx`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/store/magicCutStore.tsx`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/store/projectState.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/magicCutProjectService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/magicCutBusinessService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/TimelineEditService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/TimelineOperationService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/export/videoExportService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/export/strategies/DesktopExportStrategy.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/AssetCacheService.ts`

- [ ] **Step 6.1: 用统一资产协议替换时间线中的旁路资源引�?*

- [ ] **Step 6.2: 让导出链路走真实桌面能力而非临时实现**

- [ ] **Step 6.3: 统一模板、工程保存和恢复逻辑**

- [ ] **Step 6.4: 补齐时间线编辑与导出验证**

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-magiccut/src/services
pnpm typecheck
pnpm tauri:build
```

Expected:

- 时间线编辑逻辑测试通过
- 桌面导出路径可构�?- 资产引用一�?
**完成定义�?*

- `MagicCut` 成为真正可生产内容的剪辑引擎
- 与项目、资产、导出、桌面能力打�?
**前置依赖�?* `Step 1`、`Step 2`、`Step 3`

**可并行：**

- 可与 `Step 5`、`Step 7`、`Step 8` 并行
- 推荐独立 owner，仅持有 `packages/sdkwork-magic-studio-magiccut/*` 写集

---

### Step 7: Canvas 引擎闭环

**目标能力闭环�?*
完成后，`Canvas` 具备节点创建、连接、执行、结果入资产、历史管理和向其它引擎输出的完整工作流闭环�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-canvas/src/pages/CanvasPage.tsx`
- Modify: `packages/sdkwork-magic-studio-canvas/src/components/CanvasBoard.tsx`
- Modify: `packages/sdkwork-magic-studio-canvas/src/store/canvasStore.tsx`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasService.ts`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasGenerationService.ts`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasHistoryService.ts`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasExportService.ts`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasToCutConverter.ts`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasActionService.tsx`

- [ ] **Step 7.1: 统一节点输入输出协议**

- [ ] **Step 7.2: 让节点执行结果进入资产中�?*

- [ ] **Step 7.3: 补齐历史、恢复与转换能力**

- [ ] **Step 7.4: 验证“画�?-> 资产 / 时间线”输出闭�?*

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-canvas/src/services
pnpm exec vitest run packages/sdkwork-magic-studio-canvas/src/store
pnpm typecheck
```

Expected:

- 节点执行与历史逻辑通过
- 结果可回写资产中�?- 可导出到下游模式

**完成定义�?*

- `Canvas` 不再只是交互画板，而是工作流引�?- 输出结果可进入工程主�?
**前置依赖�?* `Step 1`、`Step 2`、`Step 3`

**可并行：**

- 可与 `Step 5`、`Step 6`、`Step 8` 并行
- 推荐独立 owner，仅持有 `packages/sdkwork-magic-studio-canvas/*` 写集

---

### Step 8: Chat / Prompt 动作层闭�?
**目标能力闭环�?*
完成后，对话和提示词系统不只是给文本建议，而是可以把自然语言转换为项目动作、引擎动作或生成任务，成为跨模式操作层�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-chat/src/services/chatService.ts`
- Modify: `packages/sdkwork-magic-studio-prompt/src/services/promptBusinessService.ts`
- Modify: `packages/sdkwork-magic-studio-prompt/src/pages/PromptOptimizerPage.tsx`
- Modify: `packages/sdkwork-magic-studio-canvas/src/services/canvasActionService.tsx`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/magicCutPromptService.ts`
- Modify: `packages/sdkwork-magic-studio-film/src/services/filmPromptService.ts`
- Create: `packages/sdkwork-magic-studio-core/src/ai/actionIntentResolver.ts`
- Create: `packages/sdkwork-magic-studio-core/src/ai/actionIntentResolver.test.ts`

- [ ] **Step 8.1: 定义 AI 动作协议**

最少支持：

- 创建生成任务
- 绑定项目资源
- 修改引擎属�?- 插入节点 / 片段 / 镜头

- [ ] **Step 8.2: �?Chat / Prompt 输出转换为动作而不是纯文本**

- [ ] **Step 8.3: 接到三大引擎的最小动作集**

- [ ] **Step 8.4: 验证跨模式动作执�?*

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-core/src/ai/actionIntentResolver.test.ts
pnpm exec vitest run packages/sdkwork-magic-studio-canvas/src/services/canvasActionService.test.ts
pnpm typecheck
```

Expected:

- 对话结果可转换为结构化动�?- 至少能驱动一个引擎执行真实操�?
**完成定义�?*

- 对话式编辑成为平台能力，而非外围工具
- AI 真正进入创作操作�?
**前置依赖�?* `Step 3`

**可并行：**

- 可与 `Step 5`、`Step 6`、`Step 7` 并行
- 需要先冻结统一动作协议，避免三引擎各自定义动作格式

---

### Step 9: 插件运行�?V1

**目标能力闭环�?*
完成后，插件系统从“市场页面”升级为真正的扩展运行时，具�?manifest、权限、生命周期、沙箱目录和最小能力暴露�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-plugins/src/pages/PluginsPage.tsx`
- Modify: `packages/sdkwork-magic-studio-plugins/src/services/pluginsBusinessService.ts`
- Modify: `packages/sdkwork-magic-studio-plugins/src/services/index.ts`
- Create: `packages/sdkwork-magic-studio-plugins/src/types/pluginManifest.ts`
- Create: `packages/sdkwork-magic-studio-plugins/src/services/pluginRuntimeService.ts`
- Create: `packages/sdkwork-magic-studio-plugins/src/services/pluginPermissionService.ts`
- Modify: `src-tauri/src/framework/services/policy.rs`
- Create: `src-tauri/src/framework/services/plugin_sandbox.rs`

- [ ] **Step 9.1: 定义 Plugin Manifest**

至少包含�?
- `id`
- `version`
- `entry`
- `capabilities`
- `requiredPermissions`
- `sandboxScope`

- [ ] **Step 9.2: 建立插件权限申请与校验流�?*

- [ ] **Step 9.3: 建立插件沙箱目录与运行时生命周期**

- [ ] **Step 9.4: 验证插件加载与隔�?*

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-plugins/src/services
pnpm typecheck
pnpm tauri:build
```

Expected:

- 插件可发现、可安装、可加载
- 权限声明未通过则不能运�?
**完成定义�?*

- 插件从“UI 壳”升级为“平台扩展能力�?- 与桌面安全主干联�?
**前置依赖�?* `Step 4`

**可并行：**

- 可与 `Step 10`、`Step 11` 并行
- 但不能在 `policy.rs` 上与其它任务无序冲突

---

### Step 10: 工程发布、导出与安装交付闭环

**目标能力闭环�?*
完成后，项目同步、导出、发布、安装包交付形成真实闭环，`projectService` 不再是示意�?mock�?
**Files:**
- Modify: `packages/sdkwork-magic-studio-editor/src/services/projectService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/export/videoExportService.ts`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/services/export/strategies/DesktopExportStrategy.ts`
- Modify: `README.md`
- Modify: `.github/workflows/release.yml`
- Modify: `src-tauri/tauri.prod.conf.json`
- Create: `packages/sdkwork-magic-studio-editor/src/services/projectPublishService.ts`
- Create: `packages/sdkwork-magic-studio-editor/src/services/projectSyncService.ts`
- Create: `packages/sdkwork-magic-studio-editor/src/services/projectPublishService.test.ts`

- [ ] **Step 10.1: 替换 Git 同步 mock**

- [ ] **Step 10.2: 替换发布 mock**

- [ ] **Step 10.3: 统一工程导出与安装包发布说明**

- [ ] **Step 10.4: 验证发布闭环**

Run:

```bash
pnpm exec vitest run packages/sdkwork-magic-studio-editor/src/services/projectPublishService.test.ts
pnpm tauri:build
pnpm verify:release:artifacts
```

Expected:

- 工程级同步和发布链路有真实实�?- 导出与桌面发布链路一�?
**完成定义�?*

- 产品从“能创作”进入“能交付�?- 项目交付链路不再停留�?mock

**前置依赖�?* `Step 1`、`Step 4`

**可并行：**

- 可与 `Step 9`、`Step 11` 并行
- 涉及 release workflow 时需避免与其它发布改动冲�?
---

### Step 11: 性能预算与观测治�?
**目标能力闭环�?*
完成后，系统不再靠体感谈性能，而是拥有引擎、任务、导入、导出、启动等关键指标、预算和验证门禁�?
**Files:**
- Modify: `docs/架构/11-性能架构与优化标�?md`
- Modify: `package.json`
- Modify: `scripts/check-iam-route-css-budget.mjs`
- Modify: `scripts/check-desktop-bundle-self-contained.mjs`
- Create: `scripts/check-performance-budget.mjs`
- Create: `docs/reports/performance-budget-matrix.md`
- Modify: `packages/sdkwork-magic-studio-canvas/src/components/CanvasBoard.tsx`
- Modify: `packages/sdkwork-magic-studio-magiccut/src/components/MagicCutEditor.tsx`
- Modify: `src/app/bootstrap.ts`

- [ ] **Step 11.1: 固定性能预算**

至少为以下对象定义预算：

- 启动
- 路由
- 画布
- 时间�?- 导入
- 导出

- [ ] **Step 11.2: 补齐观测�?*

- [ ] **Step 11.3: 将预算纳入校验脚�?*

- [ ] **Step 11.4: 验证预算门禁**

Run:

```bash
pnpm build
node scripts/check-performance-budget.mjs
pnpm verify:iam-route-css-budget
```

Expected:

- 存在明确预算
- 构建与关键页面通过预算检�?
**完成定义�?*

- 性能具备预算、观测和门禁三件�?- 后续任何重功能接入都可量化影�?
**前置依赖�?* `Step 0`

**可并行：**

- 可与 `Step 9`、`Step 10` 并行
- 对引擎组件的性能埋点需�?`Step 5`、`Step 6`、`Step 7` 协同冻结埋点接口

---

### Step 12: 集成联调、验收与正式发布门禁

**目标能力闭环�?*
完成后，所有主干能力统一通过集成验证，系统达到“可以按架构文档声称已实现”的状态�?
**Files:**
- Modify: `docs/架构/13-测试质量-安装部署-发布运维.md`
- Modify: `docs/架构/14-行业对标-评估标准与演进路�?md`
- Modify: `docs/架构/15-架构落地步骤-并行实施计划.md`
- Create: `docs/reports/release-readiness-checklist.md`
- Create: `docs/reports/e2e-capability-acceptance-matrix.md`

- [ ] **Step 12.1: 建立端到端验收矩�?*

至少覆盖�?
- Portal -> 生成 -> 资产
- Film -> 分镜 -> 生成 -> 预览
- MagicCut -> 导入 -> 编辑 -> 导出
- Canvas -> 节点执行 -> 资产 / 时间线输�?- Chat / Prompt -> 动作 -> 引擎执行
- 插件安装 -> 权限申请 -> 运行

- [ ] **Step 12.2: 跑完整门�?*

Run:

```bash
pnpm typecheck
pnpm test
pnpm audit:services:policy
pnpm verify:sdk-mode-parity
pnpm verify:mode-style-parity
pnpm verify:release:artifacts
pnpm tauri:build
```

Expected:

- 共享主干、三大引擎、插件、发布链路全部通过统一门禁
- 不存在阻断上线的架构旁路、安全风险和关键回归
- 发布就绪清单可以明确给出“可发布 / 有条件可发布 / 不可发布”结�?
- [ ] **Step 12.3: 对照本目�?01-15 文档逐条验收**

- [ ] **Step 12.4: 形成发布就绪结论**

**完成定义�?*

- 能证明架构文档中的核心能力已经被实现，而不是只被描�?- 所有关键链路通过统一门禁
- 具备发布依据与风险说�?
**前置依赖�?* `Step 5` �?`Step 11`

**可并行：**

- 不可并行。它是最终整体验�?step�?
## 5.1 �?Step 执行卡速查

下面不是新的 step，而是�?`Step 0` �?`Step 12` 进一步压缩成可以直接派工的执行卡。团队排期、派 owner、做每日站会时，优先看本节�?
### 5.1.1 Step 0 - Step 6

| Step | 默认 owner | 建议耗时 | 最快完成路�?| 完成后解�?| 并行禁区 | 回滚重点 |
| --- | --- | --- | --- | --- | --- | --- |
| `Step 0` | 架构 owner + 集成 owner | `0.5-1` �?| 只做事实冻结，不做功能延展；命令、风险、禁止项一次定�?| `Step 1-4` | 禁止任何功能 lane 提前改共享协�?| 若门禁脚本影响现有开发，先回滚脚本绑定，不回滚风险台�?|
| `Step 1` | Lane A | `2-3` �?| 先出 `ProjectGraph` 协议和测试，再接三引擎绑定，最后清�?`projectService` mock 主路�?| `Step 5`、`Step 6`、`Step 7`、`Step 10` | 禁止�?`Step 10` 同时�?`projectService.ts` | 若新图谱不兼容旧工程，先保留迁移层，不回滚统一模型方向 |
| `Step 2` | Lane B | `2-3` �?| 先冻结资�?identity 和目录结构，再替换三引擎裸路径引用，最后补引用关系 | `Step 5`、`Step 6`、`Step 7`、`Step 10` | 禁止引擎在资产协议未冻结前自造临时引用格�?| 若资产迁移影响旧项目，先保留读取兼容，不回滚资产主键与目录标�?|
| `Step 3` | Lane C | `2-3` �?| 先冻�?AI Task 协议，再收拢 `genAIService` 主路径，最后让 Chat / Prompt / Video 接任务中�?| `Step 5`、`Step 6`、`Step 7`、`Step 8` | 禁止三个引擎在协议未冻结前各自接模型 | �?SDK 主路径短期不稳，可临时保�?fallback，但必须显式受控 |
| `Step 4` | Lane D | `2-3` �?| 先收紧路径白名单，再做命令风险分级，最后让高风险能力统一�?policy | `Step 9`、`Step 10` | 禁止插件和发布团队提前依赖未定的权限模型 | 若白名单过严影响关键功能，先放宽到工作区级，不回到全局 `**` |
| `Step 5` | Lane E | `3-5` �?| 先打通项目和资产，再�?AI 任务，最后闭环脚本到分镜到预�?| `Step 12` �?Film 主链�?| 禁止�?Film 内再造项目模型、任务模型、资产模�?| 若结构化 Film 模型影响旧页签，先加兼容适配，不回滚主模�?|
| `Step 6` | Lane F | `3-5` �?| 先替换时间线资源引用，再接真实导出，最后做模板和恢�?| `Step 12` �?MagicCut 主链�?| 禁止保留旁路资源路径作为主引�?| 若真实导出链路不稳，允许保留受控降级导出，但必须通过桌面策略�?|

### 5.1.2 Step 7 - Step 12

| Step | 默认 owner | 建议耗时 | 最快完成路�?| 完成后解�?| 并行禁区 | 回滚重点 |
| --- | --- | --- | --- | --- | --- | --- |
| `Step 7` | Lane G �?Canvas owner | `3-5` �?| 先冻结节点输入输出协议，再接资产落库，最后补历史恢复和导出到下游 | `Step 12` �?Canvas 主链�?| 禁止 Canvas 继续只保留演示级本地执行 | 若转换链路不稳，先保留资产输出，再逐步打开到时间线转换 |
| `Step 8` | Lane C 下半�?| `2-3` �?| 先定义动作协议，再做 `actionIntentResolver`，最后只接三引擎最小动作面 | `Step 12` �?Chat/Prompt 主链�?| 禁止在三引擎各自定义动作格式 | 若自然语言解析稳定性不足，先限制在最小动作集，不回退为纯文本建议 |
| `Step 9` | Lane D 下半�?| `2-4` �?| 先定 manifest，再做权限校验和沙箱目录，最后接生命周期 | `Step 12` 的插件主链路 | 禁止跳过权限声明直接运行插件 | 若插件运行时不稳，先限制能力集，不回�?manifest 与权限框�?|
| `Step 10` | 发布 owner + Lane G 协作 | `2-4` �?| 先拆�?`projectSyncService / projectPublishService`，再统一导出与发布说明，最后接 release workflow | `Step 12` 的交付主链路 | 禁止�?`Step 1` 抢改 `projectService.ts`；禁止与其它团队并发修改 release workflow | 若正式发布链路异常，先回�?workflow 绑定，不回滚真实同步 / 发布服务抽象 |
| `Step 11` | 性能 owner | `1-2` �?| 先定预算矩阵，再补埋点，最后接预算脚本门禁 | `Step 12` 的性能门禁 | 禁止在预算未定时就宣称性能达标 | 若预算太激进导致频繁阻断，先调阈值，不移除预算检�?|
| `Step 12` | 架构 owner + QA owner | `1-2` �?| 先按矩阵跑主链路，再对照 01-15 文档逐条过账，最后给 Go/No-Go 结论 | 正式发布 | 禁止边验收边继续改共享协�?| 若发现阻断问题，回滚到对�?lane 修复，不在验�?step 内堆临时补丁 |

### 5.1.3 �?Step 退出判�?
每个 step 结束时，owner 必须能在站会上用一句话回答下面的问题：

- `Step 0`：后续所�?lane 是否已经基于同一套命令、风险和禁止项开工？
- `Step 1`：三大引擎是否已经共享同一 `ProjectGraph`，且不再新增私有项目主模型？
- `Step 2`：所有关键资源是否已经以 `assetId` 为主引用，而不是裸路径�?- `Step 3`：AI 是否已经以统一任务中心为主路径，直连模型是否只剩受�?fallback�?- `Step 4`：桌面高风险能力是否已经全部进入最小权限和策略校验�?- `Step 5`：Film 是否已经形成“脚�?-> 分镜 -> 生成 -> 预览”的真实闭环�?- `Step 6`：MagicCut 是否已经形成“导�?-> 编辑 -> 导出”的真实生产链路�?- `Step 7`：Canvas 是否已经形成“节�?-> 执行 -> 资产 -> 下游输出”的真实闭环�?- `Step 8`：Chat / Prompt 是否已经能驱动真实工程动作，而不是只返回文本�?- `Step 9`：插件是否已经具�?manifest、权限、沙箱、生命周期四件套�?- `Step 10`：项目同步、发布、导出、安装是否已经从 mock 进入真实交付链路�?- `Step 11`：性能是否已经具备预算、观测、门禁，而不是口头目标？
- `Step 12`：是否已经能用统一证据证明 01-15 架构文档中的核心能力真正实现�?
## 6. 并行执行建议

### 6.1 最快并行拓�?
推荐使用 7 条工作流并行推进�?
| Lane | 负责范围 | 推荐 step |
| --- | --- | --- |
| A | 工程图谱与工作区 | `Step 1` |
| B | 资产与存�?| `Step 2` |
| C | AI 主干与动作协�?| `Step 3`、`Step 8` |
| D | 桌面安全与运行时 | `Step 4`、`Step 9` |
| E | Film 引擎 | `Step 5` |
| F | MagicCut 引擎 | `Step 6` |
| G | Canvas + 发布性能 QA | `Step 7`、`Step 10`、`Step 11` |

注意�?
- `Step 8` 必须�?`Step 3` 的动作协议冻结后再放�?Lane C 下半程�?- `Step 9` 不能早于 `Step 4` 的权限白名单�?- `Step 10` 不要�?`Step 1` 同时�?`projectService.ts`，应�?`Step 1` 完成后接手�?
### 6.2 推荐波次

**Wave 0**

- `Step 0`

**Wave 1**

- `Step 1`
- `Step 2`
- `Step 3`
- `Step 4`

**Wave 2**

- `Step 5`
- `Step 6`
- `Step 7`
- `Step 8`

**Wave 3**

- `Step 9`
- `Step 10`
- `Step 11`

**Wave 4**

- `Step 12`

### 6.3 快速并行落地的具体方法

1. 先冻结共享协议，再放引擎并行�?2. 按写集切人，不按技术层切人�?3. 每个 lane 只拥有一个主写目录，减少冲突�?4. 每天至少一次集成到共享集成分支，不等到所�?step 都做完才合并�?5. 每个 step 的验证命令必须在�?lane 内先跑完，集成分支只做二次确认，不替代本地验收�?
### 6.4 不建议的并行方式

- 让多个团队同时改 `projectService.ts`、`policy.rs`、`genAIService.ts` 这类主干文件�?- 在未冻结 AI 任务协议前，让三个引擎各自接 AI�?- 在未收紧桌面权限前，提前做插件运行时�?- 在导出和发布仍是 mock 的情况下就开始做正式运营流程�?
### 6.5 极速并行实�?SOP

如果目标是“最快且完整地把 step 并行执行完毕”，建议直接按以下节奏组织：

1. `Step 0` 用半天到一天冻结：统一命令、风险台账、共享文�?owner、集成分支策略一次定死�?2. `Wave 1` 只允许共享主�?lane 工作：`Step 1`、`Step 2`、`Step 3`、`Step 4`，此时禁止引擎团队提前接入未冻结协议�?3. �?`ProjectGraph`、资产协议、AI Task 协议、桌面白名单四个共享契约全部冻结后，再同时放开 `Wave 2` �?`Film`、`MagicCut`、`Canvas`、`Chat/Prompt`�?4. `Wave 3` 中的 `Step 9`、`Step 10`、`Step 11` 只在共享文件 owner 明确移交后开始，避免�?`policy.rs`、`projectService.ts`、发布脚本上撞车�?5. `Step 12` 必须在所�?lane 完成各自本地门禁后再启动；集成分支只做统一验收，不替代�?lane 自验�?
推荐每日节奏�?
| 时间�?| 动作 | 目的 |
| --- | --- | --- |
| `09:30` | 共享契约同步会，只讨论主干协议变�?| 防止 lane 基于不同接口开�?|
| `12:00` | owner 提交半日状态与阻塞 | 提前暴露冲突和依赖缺�?|
| `15:00` | 集成分支第一次合�?| 让问题在当天暴露，而不是周末才暴露 |
| `18:00` | �?lane 跑本地门禁并回填证据 | 保证第二天开始前主干仍然可控 |

共享文件锁建议如下：

| 共享文件 / 目录 | 默认 owner | 解锁条件 |
| --- | --- | --- |
| `packages/sdkwork-magic-studio-editor/src/services/projectService.ts` | `Lane A` 先持有，`Step 10` 再移�?| `Step 1` 合并并冻结项目协议后 |
| `packages/sdkwork-magic-studio-core/src/ai/genAIService.ts` | `Lane C` | `Step 3` 协议冻结并给出受�?fallback 规则�?|
| `src-tauri/src/framework/services/policy.rs` | `Lane D` | `Step 4` 白名单和风险分级冻结�?|
| `.github/workflows/release.yml` | 发布 owner | `Step 10` 开始时独占 |
| `docs/架构/15-架构落地步骤-并行实施计划.md` | 架构 owner | 仅允许架�?owner 合并结构性调�?|

### 6.6 推荐团队编制

为了把并行效率拉满，同时把返工率压到最低，推荐最少采用以下角色模型：

| 角色 | 数量建议 | 责任 |
| --- | --- | --- |
| 架构 owner | `1` | 冻结共享协议、裁决边界、维�?`Step 0 / 12 / 15` |
| Lane owner | `7` | 各自对一个写集和一个能力闭环负责，禁止横跨抢改 |
| 集成 owner | `1` | 维护共享集成分支、安排每日合流、记录阻�?|
| QA / 验收 owner | `1-2` | 维护 E2E 矩阵、复现主链路、收集发布证�?|
| 安全 reviewer | `1` | 专盯 `Step 4 / 9 / 10 / 12`，防止高风险改动漏过 |
| 性能 reviewer | `1` | 专盯 `Step 11 / 12`，控制预算回退 |

### 6.7 关键路径压缩建议

从总耗时看，真正的关键路径不是所�?step，而是�?
`Step 0 -> Step 1 / Step 2 / Step 3 -> Step 5 / Step 6 / Step 7 / Step 8 -> Step 10 / Step 11 -> Step 12`

想把总周期压到最短，建议只做以下几件事：

1. �?`Step 0` 严格限制�?`1` 天内完成，不允许在基线冻结阶段继续讨论功能设计�?2. `Step 1`、`Step 2`、`Step 3` 必须在第一时间先产出协议文档和测试，再做业务接线，避免下游等待太久�?3. `Step 5`、`Step 6`、`Step 7` 只做“能形成完整主链路”的最小闭环，深度优化全部放在主链路跑通之后�?4. `Step 8` 先只接三引擎最小动作面，不追求一步覆盖所有自然语言操作�?5. `Step 10` �?`Step 11` 可以�?`Wave 2` 后半段提前预热，但只有在 owner 正式移交共享文件时才允许合并�?6. 所有非主链路增强项都放入二�?backlog，不得挤�?`Step 12` 的验收窗口�?7. 每个 lane 采用“先协议、后实现、再收尾”的三段式节奏，协议冻结后立即通知下游开工�?
### 6.8 并行完成的最低纪�?
并行效率高不高，不取决于开多少人，而取决于是否遵守以下最低纪律：

1. 任何共享文件在任一时刻只能有一个主 owner�?2. 任何 lane 当天没有跑完本地门禁，不得合入共享集成分支�?3. 任何 step 只要还保留主路径 mock，就不能宣称�?step 完成�?4. 任何 fallback 只要没有显式开关、审计点和退出计划，就不能进入主干�?5. 任何验收缺口都必须回到所�?lane 处理，不允许�?`Step 12` 临时拼补丁�?
### 6.9 2026-04-07 Step 01 基线冻结后的实施入口

本轮 `Step 01` 审计完成后，当前实施入口进一步明确为�?
1. 当前仓库分支�?`main`，且工作区存在大面积已修改与未跟踪文件�?2. 这意味着共享主干代码虽然已明确了优先级和 owner，但当前工作区不适合作为 `projectService.ts`、`genAIService.ts`、`policy.rs`、`default.json` 的直接并行改造现场�?3. 因此，`Step 02-05` 的下一轮真正代码实施，必须先满足以下准入条件：
   - 共享文件 owner 与移交顺序已冻结
   - 当前轮改动不会覆盖现有未提交的大面积工作区变�?   - review / release 文档已经承认这一实施风险，而不是假装不存在

当前确认的共享文�?owner 如下�?
| 共享文件 | 当前 owner | 移交时机 |
| --- | --- | --- |
| `packages/sdkwork-magic-studio-editor/src/services/projectService.ts` | Lane A | `Step 02` 协议冻结后移�?`Step 11` |
| `packages/sdkwork-magic-studio-core/src/ai/genAIService.ts` | Lane C | `Step 04` 协议冻结后只允许动作层按协议消费 |
| `src-tauri/capabilities/default.json` | Lane D | `Step 05` 完成后只允许收紧，不允许放宽 |
| `src-tauri/src/framework/services/policy.rs` | Lane D | `Step 05` 完成后可受控借写�?`Step 10/11` |
| `packages/sdkwork-magic-studio-plugins/src/services/pluginsBusinessService.ts` | Lane D2 | `Step 10` 全程持有 |

本轮基线冻结同时确认�?`Step 02-05` 的推荐推进顺序：

1. `Step 02` 项目图谱与工程协�?2. `Step 03` 资产与存储主�?3. `Step 04` AI Gateway 与任务中�?4. `Step 05` 桌面权限与运行时治理

只有在上述共享主干进入真实实现后，才允许�?`Step 06-11` 从“等待协议”切换到“闭环落码”�?
## 7. 每个 step 的交付件清单

所�?step 完成时必须同时交付：

- 代码变更与明确写集归�?- 自动化测试或契约校验结果
- 对应架构章节映射说明
- 输入 / 输出工件说明
- mock / fallback / 兼容层处理结�?- 风险说明
- 回滚方案
- 文档更新
- 演示路径
- 合并门禁证据

缺任一项，都不�?step 完成�?
## 8. 最终判�?
如果严格按本文件执行，Magic Studio V2 的架构落地将不再是抽象倡议，而是�?
- 有主�?- 有节�?- 有边�?- 有并行方�?- 有验收标�?
这也是把“架构文档写得很好”真正转化成“产品能力完整实现”的必要条件�?