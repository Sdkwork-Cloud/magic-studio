// Re-export common types to avoid circular dependencies
export type { 
    AspectRatio,
    MediaType,
    ImageStyle,
    GenerationConfig,
    GeneratedResult,
    ImageTask
} from 'sdkwork-react-commons';
